# GOL Web版｜開発進捗表

## 記法・ルール -------------

**役割:** 日々のGOL-Web版の進捗報告（整理されたもの）。概要・一覧・サマリー程度を簡潔に記録

記法・ルール → `docs-markdown-conventions.mdc`（`~/.claude/CLAUDE.md` 経由で AI に自動適用済み）

## 固定枠 --------------​

---

### 260408-Wed --------------

#### 260408-Wed｜ToDoサブ全開/全閉と習慣低頻度UIの改善を main へ反映

**記載** Cursor

**実施内容:**

- ToDoサマリー・日誌カンバンの各見出しに「サブ全開」「サブ全閉」を追加し、列単位でサブタスク展開/折りたたみを操作できるようにした

- 習慣管理の低頻度設定を強化し、編集モーダルにも「低頻度エリアに表示する」を追加。子習慣は単独で低頻度にできない仕様に合わせて管理モーダル/編集モーダルで無効化した

- 子習慣モーダルの視認性を微調整（無効ボタンはより薄いグレー、注意文は明度を上げ、説明文はチェックボックスと同等の明度へ）

- `.cursor/plans/` をルート `.gitignore` に追加して、プランファイルが追跡対象に出ないよう整理した

- 誤って別ブランチで行った作業は cherry-pick と main 取り込みで整合を回復し、対象ブランチへ push まで完了した

---

### 260406-Mon --------------

#### 260406-Mon｜ヘッダー未確定のゴルド表記を GOLD 色（text-gold）に統一

**記載** Cursor

**実施内容:**

- **`collapsible-dashboard-header.tsx`**: 未確定行の `+○G` 部分に **`text-gold`**（`globals.css` の `--color-gold`）を適用し、ヘッダー右上の GOLD 表示と色を揃えた

---

#### 260406-Mon｜ヘッダー「未確定」スコアの合算・30日窓・JST

**記載** Cursor

**実施内容:**

- **ダッシュボード `page.tsx`**: 未確定 `daily_logs` を **JST の今日から30日前**（`getDateStringDaysAgoJST`）以降に限定し、`calculateDayDeltas` を並列実行して `sumDayDeltas` でヘッダー表示。件数 **30超** は内訳省略の件数表示。**合計デルタが全 0** のときはヘッダー非表示

- **`pending-header-constants.ts` / `score-calculator.ts` の `sumDayDeltas` / ヘッダー UI（オーバーフロー文言）**、**`2-support-of-progress.md`**（案1＋ガード・一括更新注意・JST 境界の記載）

---

#### 260406-Mon｜.gitignore に Cursor デバッグログを除外

**記載** Cursor

**実施内容:**

- **ルート `.gitignore`**: `.cursor/debug*.log` を追加し、デバッグセッション用 NDJSON ログをコミット対象外にした

---

#### 260406-Mon｜RouterRefresh の refreshQuiet と 2番チェックリスト分離

**記載** Cursor

**実施内容:**

- **`router-refresh-context`**: `refreshQuiet`（`router.refresh` のみ・全画面 pending なし）を追加。`refresh` は従来どおり `useTransition`＋オーバーレイ

- **`habit-list` / `journal-impression-sections` / `todo-summary-tab`（ステータス変更）**: 習慣・日誌デバウンス保存・Notion 取込・ToDo ステータスなど高頻度経路を `refreshQuiet` に切替え、操作のたびに全画面スピナーが出ないようにした

- **`docs/2-support-of-progress.md`**: 改修チェックリストを削り、`docs/6-todo-progress.md`（新規）へ ToDo 系リストを集約。2番は記法・RSC／`router.refresh` 補助メモなどに絞った

---

#### 260406-Mon｜0番にセッション開始メモ（260406-Mon）を追記

**記載** ユーザー

**実施内容:**

- **`docs/0-AI-prompt-memo.md`**: `## 2604` 配下に `### 260406-Mon` を追加。続きから再開するための短文・日付・3番参照の案内

---

### 260405-Sun --------------

#### 260405-Sun｜サブタスク名の一括保存・モーダル保存ボタン文言

**記載** Cursor

**実施内容:**

- **`subtask-rows.tsx`**: 行ごとの「保存」を廃止。`SubtaskEditRow` を親の `name` / `onNameChange` で制御

- **`todo-summary-tab.tsx`**: モーダル開時にサブタスク名スナップショットを保持し、下部の「保存」（旧「更新」）押下で ToDo 更新後に変更があったサブタスクのみ `todo_subtasks` を差分 update。`subtaskNameDrafts`・`mergeTodoSubtasksFromServer`（古い RSC 対策）・`localSubtasksRef` を継続利用

- **文言**: 編集モーダル主ボタンを「保存」に統一。サブタスク説明文を「保存で ToDo と一緒に書き込み」に合わせて更新

---

### 260404-Sat --------------

#### 260404-Sat｜ToDoタスク名のクリック展開（カンバン・サマリー共通化）

**記載** Cursor

**実施内容:**

- **`gol-web/components/expandable-task-title.tsx`（新規）**: 長いタスク名をデフォルト1行省略（`truncate`）、クリックで全文表示・再クリックで畳む。`onPointerDown` の `stopPropagation` でカードドラッグと競合しにくくする

- **`kanban-board.tsx`**: インライン実装から上記共通コンポーネントへ差し替え

- **`todo-summary-tab.tsx`**: 同コンポーネントを一覧カードに適用（完了時の ✅ はボタン外）。ドラッグオーバーレイのタスク名は折り返し表示

---

#### 260404-Sat｜MDからToDo一括インポート機能を実装

**記載** ClaudeCode

**実施内容:**

- **`gol-web/lib/parse-todo-markdown.ts`（新規）**: `- []` 形式のマークダウンをパースして ToDo＋サブタスク配列を返す純粋関数を作成

- **`gol-web/app/dashboard/todo-md-import-modal.tsx`（新規）**: テキストエリアにマークダウンを貼り付けてリアルタイムプレビュー→「作成する」で一括登録するモーダルを作成。サブタスク（インデント `  - ` 行）も同時登録

- **`todo-summary-tab.tsx`（変更）**: ToDoサマリータブに「MDからインポート」ボタンを追加し、上記モーダルを組み込み

---

#### 260404-Sat｜やりたいことリスト→GOL-WEB 同期の方針決定

**記載** ClaudeCode

**相談・決定内容:**

お疲れさまでした。今日は MD→ToDo まわりからタスク名の展開、ログの整理までかなり進みましたね。また続きのときに声をかけてください。

- **課題**: `やりたいこと・自動化したいことリスト.md` と GOL-WEB ToDo の二重管理を解消したい（YID-11）

- **方針**: Claude Code コマンド `/sync-todos` を作成。マークダウンファイルを読み込み Supabase に直接書き込む。GOL-WEB 側コード変更なし

- **書式**: `- [] YID-N｜タスク名｜難易度（省略可）` 形式（現状ほぼ維持）

- **未実装**: `/sync-todos` コマンド本体・`SUPABASE_SERVICE_ROLE_KEY` の取得設定が残タスク

- **詳細記録先**: `YR-11-やりたいことリストとGOL-WEBの同期.md`

---

### 260403-Fri --------------

#### 260403-Fri｜日誌移動中も未確定スコア表示・ToDoスコア記録バグ修正

**記載** ClaudeCode

**実施内容:**

- **未確定スコア常時表示**: `page.tsx`の`pendingDeltas`計算を「常に今日の日誌を基準」に変更。過去・未確定どの日誌を表示していても今日の未確定スコアがヘッダーに表示され続けるよう修正（コミット `e050a99`）

- **ToDoスコア記録バグ修正**: サマリータブのステータス変更ボタンで完了させた場合に`todo_logs`へのスコア記録が行われていなかったバグを修正。完了時に`handleTaskCompletion`・未完了に戻す際に`handleTaskUncompletion`を呼ぶよう追加（コミット `9aa3dde`）

- **仕様記録**: ToDoスコアの記録・反映タイミング仕様を`1-spec-sheet.md`に追記

---

#### 260403-Fri｜ToDo・サブタスク完了日時の手動入力機能を実装

**記載** ClaudeCode

**実施内容:**

- **共通ダイアログ作成**: `components/completed-at-dialog.tsx` を新規作成。「今すぐ完了」（現在日時）と「指定日時で完了」（datetime-local input）の2択

- **todo-summary-tab.tsx**: ステータス変更ボタン（完了時）・サブタスクチェック・編集モーダルにダイアログを統合。編集モーダルはステータスが「完了済み」の場合に完了日時フィールドを表示

- **kanban-board.tsx**: ドラッグで完了カラムへ移動・サブタスクチェックにダイアログを統合。ドラッグはダイアログ確定後に楽観的更新を実行

- **完了日時表示の括弧を削除**: `(26/04/01-水 18:42)` → `26/04/01-水 18:42`（カンバン・サマリー両方）

---

#### 260403-Fri｜低頻度ゾーン修正・サブタスクタイムラグ解消・mainデプロイ対応

**記載** ClaudeCode

**実施内容:**

- **ToDoサブタスクチェックのタイムラグ解消**: `todo-summary-tab.tsx` にローカルstateを追加し楽観的更新を実装。`router.refresh()` 待ちなしでチェックが即時反映されるよう変更

- **習慣低頻度ゾーン修正（2件）**:
  - データ定義修正：「親が低頻度なら子もセットで低頻度ゾーンに含める」よう変更し、親がチェックボックス化するバグを修正
  - 配置修正：右側エリアに `hidden sm:flex` がなくモバイルでも表示されていた問題を解消。週末除外・Comp対象外・チェック時デルタ・モバイル展開エリアを高頻度エリアと揃えた

- **mainへのcherry-pick**: featureブランチ（モバイル対応途中）で行った修正8件を `main` ブランチへ cherry-pick してデプロイ反映

- **ビルドエラー修正**: cherry-pick後に `HabitMobileDetail`・`expandedHabitId`・`getHabitPointsExpPartsUtil` がmainブランチに未定義だったためビルド失敗 → `habit-list-utils.tsx` に定義を追加して解消

- **`.gitignore` 修正**: `gol-web/env.local`（ドットなし）が untracked に出ていたため `gol-web/.gitignore` に追加

---

#### 260403-Fri｜辛口コーチングアドバイスをコンテキスト考慮型に改善

**記載** ClaudeCode

**実施内容:**

- **課題の特定**: アドバイスプロンプトに習慣・ToDoの達成状況が渡されておらず、文脈なしで一律に叱る挙動だった

- **`STRICT_COACH_SNIPPET` 改善**: 「習慣登録=意欲の証拠、責めずに本質を指摘」「叱るときは理由を添える」指示を追加（`lib/ai/personality-types.ts`）

- **`createAdvicePrompt` 拡張**: `completedHabits`・`missedHabits`・`completedTodos` の3引数を追加。プロンプトを3段構成（①できたことを認める→②理由付き指摘→③具体的アクション）に変更（`lib/ai/openai.ts`）

- **`batch/route.ts` 修正**: 習慣ログを `is_checked` で達成/未達成に分類して取得。`createAdvicePrompt` に習慣データを渡すよう変更

- **`advice/route.ts` 修正**: リクエストで `dailyLogId` を受け取り、DBから習慣・ToDoデータを取得して渡すよう変更

---

### 260401-Wed --------------

#### 260401-Wed｜確定後ToDo完了時のスコア記録先修正

**記載** ClaudeCode

**実施内容:**

- **ToDoスコア設計の調査・検討**: 確定後にToDoを完了させた場合の挙動をコードで確認。`score-calculator.ts`・`kanban-board.tsx`・`todo-summary-tab.tsx` を調査

- **設計方針を決定**: ToDoは日誌と独立した存在として扱い、確定後に完了させた場合は「今日（未確定）の日誌」に報酬を記録する。過去の確定済みスコアは変えない

- **修正実装**: `page.tsx` で `todayDailyLogId` を別途取得・`KanbanBoard`・`TodoSummaryTab` に渡す。確定済み表示中は `effectiveDailyLogId`（今日の日誌ID）を使用するよう変更。カンバンボードの `isConfirmed` ブロックも解除

- **2番メモに設計メモ追記**: `## 260401-Wed ToDoスコア設計メモ` として追記

- **M1→M5 同期**: コミット・push（`a9fbc54`）+ Clipy backup 実行

---

### 260326-Thu --------------

#### 260326-Thu｜モバイルレスポンシブ対応 進捗確認・コミット

**記載** ClaudeCode

**実施内容:**

- **260325-Wed の進捗メモ追記**: Playwright モバイル設定・日誌・カンバン対応内容を 3番 に記載

- **モバイルレスポンシブ対応タスク定義**: 習慣リストの固定幅対応・タッチターゲットサイズ確認・子習慣インデント対応の3タスク作成

- **全変更をコミット**: `c070a99` として Playwright 設定・レスポンシブ対応・ドキュメント更新を反映。usage 制限の理由で習慣リスト詳細対応は次回へ

---

### 260325-Wed --------------

#### 260325-Wed｜モバイルレスポンシブ対応（日誌・カンバン・Playwright設定）

**記載** ClaudeCode

**実施内容:**

- **Playwright モバイル設定**: Pixel 5 / iPhone 12 プロジェクト追加。`npm run test:e2e:mobile` コマンド追加。`gol-web/e2e/mobile-visual.spec.ts` 新規作成

- **日誌インプレッションエリア**: `journal-impression-sections.tsx` のテキストエリア固定高さ `h-[800px]` をレスポンシブ対応に変更

- **AI判定結果エリア**: `journal-form.tsx` の grid レイアウト `grid-cols-2` → `grid-cols-1 sm:grid-cols-2` に変更（モバイルでは1列、small以上で2列）

- **カンバンボード**: `todo-summary-tab.tsx` の3列カンバン `md:grid-cols-3` → `sm:grid-cols-2 md:grid-cols-3` に変更（モバイルでは2列、medium以上で3列）

---

### 260324-Tue --------------

#### 260324-Tue｜設定ページ削除・導線統一・管理者アカウント追加

**記載** ClaudeCode

**実施内容:**

- **習慣管理・ToDo管理・権利設定の導線統一**: マイページからの設定ページ（`/settings/habits`・`/settings/todos`）を削除。ダッシュボードの1系統に統一し、改修時のメンテコスト削減

- **管理者アカウント追加**: `dta2.web@gmail.com` を `NEXT_PUBLIC_ADMIN_EMAILS` に追加（`.env.local` + Vercel 環境変数）

- **CLAUDE.md ルール強化**: チェックリスト実装ワークフローの記述を「実装」限定から「作業の種類を問わず」に拡張

---

#### 260324-Tue｜ヘッダー日付ナビ・未確定スコア表示・習慣低頻度エリア分離

**記載** ClaudeCode

**実施内容:**

- **ヘッダー日付ナビゲーション**: 展開・折りたたみ両状態に前日・翌日ボタンを追加。折りたたみ時に未確定ゴルド・EXPを日付横に表示

- **UIスタイル調整**: 日付ボタンに `mx-[5px]` 余白追加・未確定テキストの位置・余白微調整（`translate-y-[2px]`・`ml-[15px]`・頭EXP `ml-[9px]`）

- **習慣 低頻度エリア分離**: `is_low_frequency` フィールドを追加。設定画面フォームと管理モーダル（日誌画面）にトグルボタンを実装。日誌画面の良習慣・悪習慣エリアに低頻度セクション（デフォルト折りたたみ）を追加。DB マイグレーション SQL 作成

---

### 260322-Sun --------------

#### 260322-Sun｜権利スコア計算・タスク枠線・日付保持・React パフォーマンス改善

**記載** ClaudeCode

**実施内容:**

- **権利スコア計算 `is_active` 対応**: 非活性権利を `score-calculator.ts` の `parseRightsPoints()` でスキップ（ポイント 0 返す）。前日実装の `is_active` フラグをスコア計算にも反映

- **期限切れタスク枠線色変更**: `border-red-700` → `border-rose-300` に変更（`todo-summary-tab.tsx`・`kanban-board.tsx`）。赤の強すぎる配色をソフトなローズに調整

- **日誌→権利設定遷移の改善**: 未保存の権利カウントがある場合に確認ダイアログを表示。`returnDate` クエリパラメータで「設定→ダッシュボードに戻る」時の日付を保持

- **大型リファクタリング** (`refactor/260322-apply-react-best-practice` ブランチ): `memo()` 追加・`lazy()+Suspense` 導入・`useMemo` 化・`Promise.all()` 並列化・コンポーネント分離（`subtask-rows.tsx`・`habit-list-utils.tsx` 新規作成）

---

### 260321-Sat --------------

#### 260321-Sat｜権利の非活性化・AI読み上げ・UI改善

**記載** ClaudeCode

**実施内容:**

- **権利（本日の利用ゴルド）非活性化**: 設定画面に活性/非活性トグルを追加。非活性権利は日誌画面で非表示・スコア計算対象外（`rights_config` JSON に `is_active` フィールドで保存）

- **AI読み上げ機能**: 総評・あらすじ（これまでの冒険/これからの冒険）・アドバイスに音声読み上げボタンを設置。速度 1.0x / 1.3x / 2.0x 切り替え対応（`lib/use-speech.ts` 共通フック）

- **設定画面リンク修正**: settings 以下4ページの「ダッシュボードに戻る」リンクが `/mypage` を向いていたのを `/dashboard` に修正

- **統計グラフ表示期間セレクター**: フォントサイズ調整・両グラフ共通であることの注釈追加

- **AI判定 総評**: 文字サイズを `text-lg` → `text-base` に変更

- **アドバイス 名前行直後の空き**: `collapseFirstBlank` オプションで名前行直後の空行を詰める処理を追加

---

#### 260321-Sat｜統計画面｜表示期間セレクター改善

**記載** ClaudeCode

**実施内容:**

- **フォントサイズ調整**: 「表示期間」ラベル・セレクターを `text-2xl` → `text-base` に変更（`stats-tab.tsx` line 324）

- **注釈追加**: セレクター横に「※ ゴルド・EXP推移グラフと習慣達成率グラフ、両方の期間を切り替えます」を追記。`text-zinc-300` で表示

---

### 260320-Fri --------------

#### 260320-Fri｜統計画面｜進捗サマリー期間拡張・習慣達成率グラフ期間拡張

**記載** ClaudeCode

**実施内容:**

- **進捗サマリー期間拡張**: 週間/月間の固定2列表示 → 1週間・1ヶ月・3ヶ月・6ヶ月・1年のボタン切り替え制に変更（`stats-tab.tsx`）

- **グラフ表示期間拡張**: 期間セレクターに「過去180日間」「過去1年間」を追加

- **API上限拡張**: 3本の stats API（`points-exp`, `habits-completion`, `todos-completion`）の上限を90日 → 365日に変更

---

#### 260320-Fri｜仕様書・進捗メモ更新（AI挨拶ルール・項目数表示の調査記録）

**実施内容:**

- **1-spec-sheet.md**: ユーザーが「おはよう」と言ったときは辰彦大先生のように「おはようございます」と返す、を確定ルールとして追記

- **2-support-of-progress.md**: 「項目数の表示」についてコード調査結果をファイル先頭に追記（日誌の `HabitList` は良／悪／ボーナスで `(n件)`、設定 › 習慣は良／悪のみ、ルート `/mypage` に件数なし）

---

#### 260320-Fri｜`:bp` バイパスモード実装・ステータスライン表示対応

**記載** ClaudeCode

**実施内容:**

- `:bp` コマンドで全許可を自動承認するバイパスモードのトグル機能を実装（`~/.claude/hooks/bypass-toggle.sh` / `bypass-permission.sh`）

- `settings.json` に `UserPromptSubmit` / `PermissionRequest` フックを追加、`Stop` 時のアラート音（Glass.aiff）も設定

- トリガーを `!bp`（シェルコマンド扱いで動作しない）→ `:bp` に修正

- ステータスラインに bp:ON 状態を `🔓 bp:ON` として表示する機能を追加（`~/.claude/statusline-command.sh`）

---

### 260320-木 --------------

#### 260320-木｜習慣管理UI・機能強化パック（7タスク完了）

**実施内容:**

- API バグ修正：習慣編集時の副見出し（description）削除が反映されない問題を修正（`updatePayload.description = descValue` で常に含める）

- UI 改善：トースト文言統一。週末除外・Comp対象外のトグル時に「変更しました」で統一

- 習慣リスト：アコーディオン見出しに件数表示（`(${goodHabits.length}件)` など）

- 親習慣設定解放：親習慣に週末除外・Comp対象外を設定可能に（削除: `isParentWithChildren disabled`、警告文廃止）

- 非活性・活性機能：`is_active` フィールド追加（DB・型・API・UI フィルタリング）。マイページで toggle ボタン追加、日誌画面から非活性習慣を非表示化

- D&D 並び替え実装：マイページと日誌画面の習慣管理モーダル双方で dnd-kit を使った drag-and-drop 対応。ドラッグハンドル・リアルタイム並び替え・`display_order` 更新

- チェックリスト ワークフロー確立：全プロジェクト共通ルール（実装完了 → 日付タグ、ユーザー確認 → チェックマーク）を `~/.claude/CLAUDE.md` Rule 11 に記載・130-2 ドキュメントに併記

---

#### 日付見出し

- 降順（新しい日付が上）

- フォーマット: `### YYMMDD-曜日`

#### 記載項目

- **実施内容**: その日やったこと（箇条書き）

- **決定事項**: 重要な意思決定

- **次回予定**: 次にやること

#### 更新タイミング

- 作業終了時にAIが更新

- 1日1セクション

#### チェックリストの日付記載ルール

**⚠️ 重要: チェックリストにチェックが入ったら、必ず日付も追加すること**

- フォーマット: `- [x] 項目名 \`YYMMDD-曜日\``

- 例: `- [x] ログイン/サインアップ完了 \`251107-金\``

- 日付は該当項目が完了した日付を記載

- 履歴から過去分も追記する（Phase 1など既に完了している項目も含む）

- **AIはチェックリストを更新する際、必ず日付も同時に追加すること**

#### 住み分け

**このファイル（3-project-progress.md）:**

- 役割: 概要、一覧、サマリー程度
- 記載内容: 実施内容の箇条書き（要点のみ）、決定事項、成果物リスト、学んだこと（簡潔に）
- 記述レベル: クリーンで読みやすい。「何を作ったか」を記録

**4-dev-log.md（開発ログ）:**

- 役割: 詳細にやったことを全て記録
- 記載内容: 使用したコマンド（全て）、コードの詳細、実装手順（ステップバイステップ）、エラーと解決方法、学習メモ（詳細な技術解説）
- 記述レベル: 雑多でOK、クリーンにする必要なし。「どうやって作ったか」を記録

---

#### 参照：ランク名定義（世界観別）

実装: `gol-web/lib/rank-utils.ts`。レベル閾値は身体・頭脳・精神の各EXPが Lv1→50, 2→100, 3→200, 4→400, 5→600, 6→900, 7→1200, 8→1600, 9→2000, 10→2500。

**ヨウテイモード（ゴースト・オブ・ヨウテイ風）**
- Lv1: 無名の凡人、Lv2: 見習い修行者、Lv3: 修行者、Lv4: 兵法者、Lv5: 武芸者、Lv6: 武者、Lv7: 剣豪、Lv8: 達人、Lv9: 影、Lv10: 怨霊

**ドラクエモード**
- Lv1: 駆け出し、Lv2: 村の若者、Lv3: 冒険者、Lv4: 鉄の戦士、Lv5: 銅の剣士、Lv6: ロトの末裔、Lv7: 銀の勇者、Lv8: 金の勇者、Lv9: ロトの化身、Lv10: 伝説の勇者

---

### 進行チェックリスト --------------​

**進捗: 6/6 完了（100%）**

#### Phase 1 実装チェックリスト

**進捗: 6/6 完了（100%）** 🎉

- [x] ログイン/サインアップ完了 `251107-金`

- [x] ダッシュボードヘッダー `251108-土`

- [x] タブナビゲーション（日誌/ToDo） `251108-土`

- [x] カンバンボードモック `251108-土`

- [x] 習慣リストUI `251112-水`

- [x] 日誌フォームUI `251112-水`

#### Phase 2 実装チェックリスト

**進捗: 9/9 完了（100%）** 🎉

- [x] データベース設計書作成（06-database-schema.md） `251113-水`

- [x] Supabaseテーブル作成（6テーブル） `251113-水`

- [x] RLS（Row Level Security）設定 `251113-水`

- [x] 新規ユーザー登録時のprofiles自動作成機能 `251113-水`

- [x] ダッシュボードでprofilesデータ表示（DB連携） `251114-木`

- [x] 習慣リストのデータベース連携（habits/habit_logs） `251114-木`

- [x] 日誌フォームのデータベース連携（daily_logs） `251114-木`

- [x] ToDoカンバンのデータベース連携（todos/todo_logs） `251128-金`

- [x] ドラッグ&ドロップ機能実装（@dnd-kit/core） `251128-金`

#### Phase 3 実装チェックリスト

**進捗: 7/7 完了（100%）** 🎉

- [x] ToDoサマリータブの実装（CRUD管理画面） `251128-金`
  - [x] アクティブタスク一覧表示 `251128-金`
  - [x] 完了済みタスク一覧表示 `251128-金`
  - [x] タスクの詳細情報表示（サブタスク、期限、報酬など） `251128-金`
  - [x] ToDoの追加機能（「+ 新規タスク」ボタン） `251128-金`
  - [x] ToDoの編集機能（「📝編集」ボタン） `251128-金`
  - [x] ToDoの削除機能（「🗑削除」ボタン） `251128-金`
  - [x] 検索機能（オプション） `251128-金`

- [x] ToDoサブタスク（子チェックリスト）機能の実装 `251128-金`
  - [x] todo_subtasksテーブルの作成（データベース設計） `251128-金`
  - [x] サブタスクのCRUD操作（作成・読み取り・更新・削除） `251128-金`
  - [x] サブタスクのUI表示（展開/折りたたみ、チェックボックス） `251128-金`
  - [x] サブタスクの追加・編集・削除機能 `251128-金`
  - [x] サブタスクの進捗管理（完了状態の保存） `251128-金`

- [x] 完了済みタスクの報酬計算・反映機能 `251209-火`
  - [x] タスク完了時の報酬計算ロジック（ポイント/EXP） `251209-火`
  - [x] todo_logsへの記録機能 `251209-火`
  - [x] profilesテーブルのポイント/EXP更新機能 `251209-火`

- [x] 習慣の追加機能実装（モーダル方式） `251209-火`
  - [x] 「[+ 習慣を追加]」ボタンに機能実装 `251209-火`
  - [x] 習慣追加モーダルの作成 `251209-火`
  - [x] 習慣追加フォーム（習慣名、種類、ポイント、EXP、入力タイプなど） `251209-火`
  - [x] データベースへの保存機能 `251209-火`

- [x] 習慣管理画面の実装（Phase 4で完了） `251209-火`
  - [x] 習慣一覧表示・編集機能 `251209-火`
  - [x] 習慣の削除機能 `251209-火`
  - [x] 習慣のカスタマイズ機能（ポイント/EXP/入力タイプなど） `251209-火`

- [x] AI判定機能の基盤実装 `251209-火`
  - [x] OpenAI API統合（GPT-4o mini） `251209-火`
  - [x] APIキーの環境変数設定 `251209-火`
  - [x] API呼び出し用のユーティリティ関数作成 `251209-火`

- [x] AI判定機能の実装 `251209-火`
  - [x] 体調スコア判定（0-100点） `251209-火`
  - [x] 気分スコア判定（0-100点） `251209-火`
  - [x] ポイント/EXP自動計算ロジック `251209-火`
  - [x] daily_logsへのAI判定結果保存機能 `251209-火`

- [x] AI生成機能の実装 `251209-火`
  - [x] アドバイス生成（厳しめコーチング） `251209-火`
  - [x] あらすじ生成（RPG物語風） `251209-火`
  - [x] 生成結果の表示機能 `251209-火`

#### Phase 4 実装チェックリスト

**進捗: 5/5 完了（100%）** 🎉

- [x] shadcn/ui導入 `251209-火`
  - [x] shadcn/uiのインストールと初期設定 `251209-火`
  - [x] 基本コンポーネントの導入（Button, Card, Dialog, Input, Textareaなど） `251209-火`
  - [x] 既存コンポーネントをshadcn/uiコンポーネントに置き換え `251209-火`

- [x] コンポーネント共通化 `251209-火`
  - [x] 共通コンポーネントディレクトリの作成（components/ui/） `251209-火`
  - [x] モーダルコンポーネントの共通化 `251209-火`
  - [x] フォームコンポーネントの共通化 `251209-火`
  - [x] カードコンポーネントの共通化 `251209-火`

- [x] レスポンシブデザイン調整 `251209-火`
  - [x] モバイル対応（スマートフォン・タブレット） `251209-火`
  - [x] ダッシュボードレイアウトのレスポンシブ対応 `251209-火`
  - [x] カンバンボードのレスポンシブ対応 `251209-火`
  - [x] モーダルのレスポンシブ対応 `251209-火`

- [x] アクセシビリティ対応 `251209-火`
  - [x] キーボードナビゲーション対応 `251209-火`
  - [x] ARIA属性の追加 `251209-火`
  - [x] フォーカス管理の改善 `251209-火`
  - [x] スクリーンリーダー対応 `251209-火`

- [x] 習慣管理画面の実装 `251209-火`
  - [x] 習慣一覧表示・編集機能 `251209-火`
  - [x] 習慣の削除機能 `251209-火`
  - [x] 習慣のカスタマイズ機能（ポイント/EXP/入力タイプなど） `251209-火`
  - [x] 習慣の並び替え機能（display_order） `251209-火`

#### Phase 5 実装チェックリスト

**進捗: 5/5 完了（100%）**

- [x] エラーハンドリング・バリデーション強化
  - [x] フォームバリデーションの実装（クライアント側・サーバー側） `251227-土`
  - [x] エラーメッセージの統一とユーザーフレンドリーな表示 `251209-火`
    - [x] Toast通知（sonner）の導入 `251209-火`
    - [x] habit-list.tsxのalert()をtoast()に置き換え `251209-火`
    - [x] journal-form.tsxのalert()をtoast()に置き換え `251209-火`
  - [x] API呼び出し時のエラーハンドリング強化 `251209-火`
    - [x] AI判定/アドバイス/あらすじAPIで詳細エラー返却 `251209-火`
    - [x] OPENAI_API_KEY未設定時は503を返すよう明示 `251209-火`
  - [x] ネットワークエラー時のリトライ機能 `251227-土`
    - [x] リトライ機能のユーティリティ関数作成（lib/api-retry.ts） `251227-土`
    - [x] 指数バックオフアルゴリズムの実装 `251227-土`
    - [x] journal-form.tsxのAI API呼び出しにリトライ機能を適用 `251227-土`
  - [x] ローディング状態の改善（スケルトンUIなど） `251227-土`
    - [x] Skeletonコンポーネントの導入（shadcn/ui） `251227-土`
    - [x] AI判定結果表示にスケルトンUIを適用 `251227-土`
    - [x] AIアドバイス・あらすじ生成にスケルトンUIを適用 `251227-土`
  - [x] パスワードリセット機能の実装 `251227-土`
    - [x] ログインページに「パスワードを忘れた場合」リンクを追加 `251227-土`
    - [x] パスワードリセット申請ページ（/forgot-password）の作成 `251227-土`
    - [x] 新しいパスワード設定ページ（/reset-password）の作成 `251227-土`
    - [x] Toast通知の追加（成功/エラー時） `251227-土`
    - [x] セッション確認ロジックの改善 `251227-土`
  - [x] フォームバリデーションの実装（クライアント側・サーバー側） `251227-土`
    - [x] バリデーション用ユーティリティ関数の作成（lib/validation.ts） `251227-土`
    - [x] AI API Routeにサーバー側バリデーションを追加 `251227-土`
    - [x] データベース操作のAPI Route作成（daily-logs, habits, todos） `251227-土`
    - [x] データベース制約との整合性確認 `251227-土`
    - [x] サーバー側バリデーション設計書の作成（08-server-validation.md） `251227-土`

- [x] パフォーマンス最適化 `260204-水`
- [x] データ取得の最適化（不要な再取得の削減、並列化） `260117-土`
  - [x] コンポーネントのメモ化（React.memo、useMemo、useCallback） `260117-土`
  - [x] 画像・アセットの最適化 `260204-水`
  - [x] コード分割・動的インポートの実装 `260117-土`
  - [x] データベースクエリの最適化 `260117-土`

- [x] 統計・分析機能の実装
  - [x] 進捗ダッシュボード（週間・月間統計） `260116-金`
  - [x] 習慣の達成率グラフ `260116-金`
  - [x] ポイント・EXPの推移グラフ `260116-金`
  - [x] ToDo完了率の可視化 `260116-金`
  - [x] エクスポート機能（CSV/JSON） `260204-水`
  - [x] インポート機能（エクスポート形式の取り込み・別アカウント移行用） `260216-月`
  **⚠️ 廃止:** 体調・気分スコアの推移グラフ（MD版で廃止のため、Web版でも廃止。260116）

- [x] テスト実装 `260216-月`
  - [x] 単体テスト（Vitest） `260116-金`
  - [x] コンポーネントテスト（React Testing Library） `260116-金`
  - [x] E2Eテスト（Playwright） `260216-月`
  - [x] API Routeのテスト `260116-金`
  - [x] データベース操作のテスト `260216-月`

- [x] デプロイ準備 `260216-月`
  - [x] 環境変数の整理とドキュメント化（.env.example、appendix/deploy-guide.md） `260216-月`
  - [x] 本番環境の設定（Supabase、OpenAI API）（手順書に記載） `260216-月`
  - [x] ビルドエラーの解消（score-calculator.ts の型修正） `260216-月`
  - [x] セキュリティチェック（RLS、APIキー保護）（手順書に記載） `260216-月`
  - [x] パフォーマンステスト（ビルド成功確認・手順書に記載） `260216-月`
  - [x] デプロイ手順書の作成（docs/appendix/deploy-guide.md） `260216-月`

#### **以下実装中止**

- [ ] MD版との同期機能実装
  - [ ] MD → Web 同期機能（Markdownファイルからデータベースへ）
  - [ ] Web → MD 同期機能（データベースからMarkdownファイルへ）
  - [ ] 手動同期ボタンの実装
  - [ ] Markdownパーサーの実装（習慣、ToDo、日誌、権利、AI判定の抽出）
  - [ ] Markdown生成ロジックの実装（データベースからMarkdown形式への変換）
  - [ ] 習慣名マッチング機能（MD版とWeb版の習慣名を一致させる）
  - [ ] 日付フォーマット変換（YYMMDD-W ↔ YYYY-MM-DD）
  - [ ] 競合解決機能（データ不一致時の処理）
  - [ ] ファイルアクセス方法の実装（GitHub API or Supabase Storage）


**注:** エラーハンドリング・バリデーション強化は完了（Phase 5の主要タスクの1つ）


## 2603 --------------

### 260319-木

**記載** ClaudeCode

#### 実施内容

- ToDoの複製機能を実装（サブタスクも含めて丸ごと複製）

- サブタスク並び替えに↑↓ボタンを追加、D&Dドラッグが反応しない問題を修正（touch-none追加）

- バグ修正: サブタスク新規作成時に進行中タスクがアクティブに戻る問題を解消

- ToDoサマリー画面でD&Dによるステータス変更・サブタスクチェックを実装

- 確定済み日誌でもToDoの状態変化（ステータス変更・サブタスクチェック）を可能に

#### 次回予定

- 未定

---

### 260318-水

#### 実施内容

- カレンダー: 週末の色を変更（土日を薄いグレーで表示）。未来日付でも週末スタイルが効くよう disabled の opacity をやめ文字色のみに
- 日付ナビ: 前日・翌日ボタンを日付枠の左右に配置。枠と「日付へ移動」の間の余白（gap-x-4）、クリック時のフォーカスリング削除（onMouseDown preventDefault）、「日付へ移動」は右端寄せ（ml-auto）
- 日付入力: アクティブなセグメントのみ背景 zinc-100・文字色白よりの薄いグレー（globals.css の WebKit 疑似要素）
- .cursor/rules: react-best-practices.mdc のシンボリックリンク追加、implementation-react.mdc（実装時は react-best-practices に準拠）と README を追加

#### 次回予定

- 未定

---

### 260306-金

#### 実施内容

- マイページ習慣管理: 良習慣・悪習慣をアコーディオンで畳めるようにした。見出しアイコン・色を日誌画面に統一（CheckCircle+シアン／AlertCircle+赤）。ボーナス習慣の枠を削除。良習慣を追加ボタンをシアン化・両ボタン文字を太字に。閉時は下向き矢印をシアンに統一。JSX余分な閉じタグ削除でBuild Error解消

#### 次回予定

- 未定

---

### 260305-木

#### 実施内容

- docs/_INDEX.md を現状に合わせて更新。ファイル名・パスを実在に修正し、プレフィックス番号順に並び替え。各ドキュメントの通称にプレフィックス番号（0〜5、z、07〜11番）を追加
- 作業終了。進捗メモ（3・4）に 260305-木 を追記。変更の add・commit・push

#### 次回予定

- 未定

---

### 260304-水

#### 実施内容

- 習慣リスト: 日付（dailyLogId）切り替え時にローカルチェック状態（localUpdates）をクリア。2番メモに習慣チェックの保存仕様（即時DB保存・日誌保存と独立）を追記
- あらすじ・辛口コーチングアドバイスをアコーディオンで折りたたみ可能に（journal-form.tsx）
- 作業終了。進捗メモ（4・5）に 260304-水 を追記。変更の add・commit・push

#### 次回予定

- 未定

---

### 260301-日

#### 実施内容

- お知らせを日付降順表示に変更（API: notice_date 降順、クライアント: 日付部分 YYYY/MM/DD でソート、fetch に cache: 'no-store'）
- 「日誌確定後にスコアは獲得されます」の注釈の明度を上げた（text-zinc-500 → zinc-300）
- 作業終了。進捗メモに 260301-日 を追記。変更の add・commit・push（コミットメッセージは箇条書き1つ、対応ブランチへ push）

#### 決定事項

- 特になし

#### 次回予定

- 未定

## 2602 --------------

### 260219-木

#### 実施内容

- 作業終了。進捗メモ（3-project-progress.md・4-dev-log.md）に 260219-木 の記載を追加。変更ファイルの add・commit・push（コミットメッセージは箇条書き1つ、対応ブランチへ push、o-manu-frontend の demo/main には直 push しない）

#### 次回予定

- 未定

---

### 260218-水

#### 実施内容

- Notion 日誌取り込み連携の実装（API Route、ダッシュボード「Notionから取り込み」ボタン、確認ダイアログ、toast 通知）。環境変数は .env.example に項目のみ（ダミー値）、実際の値は .env.local に記載する運用に整理
- .env.example: 本物の API キー・DB ID を書かない注意を追記し、NOTION 系を明らかなダミー（ntn_xxxx、xxxxxxxx-xxxx-...）に変更
- .env.local: Notion 用のキー名（NOTION_API_KEY、NOTION_JOURNAL_DB_ID）を追加（値は自分で記入）。進捗メモ（NotionMCP-Sync-memo）で「実際にアプリが読むのは .env.local」と明記
- Notion 取り込みのアカウント制限: NOTION_IMPORT_ALLOWED_EMAILS（カンマ区切り）で許可メールを指定。GET /api/notion/import/allowed で許可判定、POST で未許可時 403。ボタンは許可時のみ表示。ボタンを日誌・感想の見出しの上の段の右端に配置、ラベル「Notionから日誌と感想を取り込む」・下マージン 20px
- Notion 取り込み後の保存: 取り込み反映時・上書き確定時に即時で daily_logs に保存。「日誌を保存」で権利＋日誌本文・一言感想（journalTextsRef）をまとめて保存し、保存成功後に router.refresh() で日誌画面の全データを再取得
- .env.local から NOTION_DATE_PROPERTY_NAME=日付 を削除（未設定時デフォルトで「日付」「Date」を試すため不要）

#### 決定事項

- 環境変数の本番値は .env.local にのみ書く。.env.example はひな形のみで git にコミットする

#### 次回予定

- 未定

---

### 260217-火

#### 実施内容

- アバター画像ディレクトリ名を ghost → yotei → gy に変更。`rank-avatar.tsx` でパスを `gy` に、`avatar.md` の配置先・フォルダ構成を gy に更新
- 日誌あらすじ・アドバイス表示: これまでの冒険・これからの冒険・辛口コーチングアドバイスの3箇所で、2行ごとに空行を挿入するルールを追加。`lib/utils.ts` に `insertBlankLineEveryTwoLines` を追加、`journal-form.tsx` の `renderAiText` に `blankEveryTwoLines` オプションを追加し該当3箇所で有効化

#### 決定事項

- 特になし

#### 次回予定

- 未定

---

### 260216-月

#### 実施内容

- テスト実装（3-project-progress.md の「テスト実装」チェックリストを実施）
  - E2Eテスト: Playwright を導入。`playwright.config.ts`、`e2e/smoke.spec.ts`（トップ・ログインページのスモーク）を追加。`npm run test:e2e` で実行。Vitest から E2E を除外（`vitest.config.mjs` の exclude に `e2e` を追加）
  - データベース操作のテスト: `/api/announcements` の GET/POST をモックでテスト。`app/api/announcements/__tests__/route.test.ts` を追加（未認証401、認証済み一覧200、POST は403/400/200）
- UI・進捗まわり（同日・作業終了前）
  - ローディング表示: `app/loading.tsx`・`app/dashboard/loading.tsx`・`app/mypage/loading.tsx` を追加。カレンダー日付変更時にスピナー表示（calendar-dialog-context で navigatingToDate 時に同一スピナーをオーバーレイ）
  - アイコン統一: キャラ名・マイページプロフィールを Trophy → Swords。今日の習慣実行を Sparkles → ListChecks。良習慣見出しに CheckCircle、悪習慣見出しに AlertCircle。本日の利用ゴルドの Gift を text-gold。マイページ習慣管理を Sparkles → ListChecks
  - 見出し: 今日の習慣実行／良習慣／悪習慣の文言・フォントサイズ（日誌・感想と同一）・semibold。ToDoリスト見出しを日誌・感想と同じ text-xl sm:text-2xl font-medium に
  - 本日の利用ゴルド: 権利一覧にリストマーク「-」を先頭に表示（ul/li＋明示的「-」span）
  - 進捗表: Phase 5 の「統計・分析機能の実装」を完了に。進捗を 5/5 完了（100%）・6/6 完了（100%）に更新
  - お知らせ日付用 SQL: `docs/sql-snippet/update-announcement-notice-date.sql` を追加（notice_date を 2025/11/23-木 に更新）

#### 決定事項

- 特になし

#### 次回予定

- 今日はここで終了

---

### 260213-金

#### 実施内容

- AI判定の総評の永続表示: daily_logs.ai_reasoning カラムを追加。型定義・batch API で reasoning を DB 保存、journal-form で dailyLog から reasoning を読み込み・同期（ページ遷移・確定後も総評が残る）
- 獲得スコア UI 調整: 下段ラベル（総加算ゴルド・総減算ゴルド・今回の獲得ゴルド・総加算EXP・総減算EXP・今回の獲得EXP）の色を text-zinc-300 に統一（上段ラベルと同じ）。悪習慣は 0 でも常に「-」を表示。ExpWithIcons で全 0 のときは非表示。ゴルドと EXP の間隔調整
- 親習慣の週末除外・Comp対象外: 親習慣行から該当ボタンを削除。親習慣編集モーダルでは該当オプションを disabled にし、注釈で「子習慣で個別に設定してください」と案内
- 習慣リスト: ゴルド・EXP の加減算設定値を text-zinc-100 に変更。モーダル内の週末除外注釈・親習慣時のチェックボックス・注釈色を調整
- 習慣リストレイアウト: 週末除外・Comp対象外を右に寄せるラッパー（ml-3）。右ブロックに min-w（39→42rem、親行28→31rem）と overflow-x-auto で加減算表示が落ちないように。ゴルド/EXP 幅を 14rem に統一（全9箇所）
- 確定後も週末除外・Comp対象外はホバー時のみ表示: 確定時の opacity-60 を削除し、opacity-0 group-hover:opacity-100 を維持
- 設定画面: 物語の世界観でゴースト・オブ・ヨウテイ風を上に並び替え（ラジオ＋管理者用「〇〇風の設定」）。ニックネーム注釈を「各世界観のデフォルト名（篤、もょもと）」に変更

#### 決定事項

- 特になし

#### 次回予定

- 260211 の次回予定を参照

---

### 260212-木

#### 実施内容

- 子習慣まわりUI調整: 習慣モーダルのラベルを「親習慣（子習慣あり）」から「親習慣（子習慣を設定する）」に変更
- 親習慣選択UIの削除: 設定画面・ダッシュボード習慣モーダルから親習慣セレクトボックスと説明文を削除。設定画面の parentHabitOptions も削除（親子の設定はモーダルの「親習慣（子習慣を設定する）」からのみ）
- 親習慣見出しの見た目: フォントサイズを子習慣と同じ text-base に。開始位置をチェックボックスがある行と同じ左端に揃え（余白なし）。子習慣の pl-6 インデントは維持
- 子習慣ネスト機能のプラン更新: `.cursor/plans/子習慣ネスト機能_076a37a2.plan.md` を実装に合わせて修正（文言・設定画面に親習慣選択なし・親見出しの見た目仕様を追記）
- ToDo編集まわり: 期限ラベルの「期限（任意）」を「期限」に変更（ダッシュボードToDoモーダル・設定画面ToDo）

#### 決定事項

- 特になし

#### 次回予定

- 前日（260211）の次回予定を参照

---

### 260211-水

#### 実施内容

- 権利消費の整合性: 「本日消費ゴルド合計」と獲得スコア内訳の「権利消費（マイナス）」をクライアントの totalPoints で統一。合計を「総加算ゴルド − 総減算ゴルド = 今回の獲得ゴルド」形式で表示（EXPも同形式）
- AI判定: 判定理由の上に「総評」見出しを追加
- AI生成テキストの文字数制限: 管理者用に総評・これまでの冒険・これからの冒険・辛口コーチングアドバイスの最小・最大文字数を設定可能に。ai_output_limits テーブル・GET/PATCH API・世界観設定画面の「文字数制限」UIを追加。一括生成時に制限をプロンプトに反映
- 文字数制限の保存失敗対応: RLS に管理者用 INSERT ポリシーを追加（add-ai-output-limits.sql）。保存エラー時にサーバー詳細をトーストで表示
- 表記統一: 「- 5G」「身体 + ◯」「頭脳 + ◯」「精神 + ◯」の半角スペースを全画面で統一（habit-list, journal-form, kanban-board, todo-summary-tab, collapsible-dashboard-header, settings/habits, settings/todos）
- ToDo編集のモーダル集約: サブタスクのリネーム・削除・追加は編集モーダル内のみ。カード一覧ではサブタスクは名前＋完了日時表示のみ
- 属性ラベル: EXP_ATTRIBUTE_LABELS を「体→身体」「頭→頭脳」「心→精神」に変更（lib/types.ts）
- サブタスク三角マークのサイズ調整・サブタスクありのToDoはデフォルト展開（ToDoサマリー・カンバン）
- 子習慣ネスト機能: Planモードで実装計画を策定（親は見出しのみ・子のみチェック）。99-cursor-manual に Plan 保存先・.cursor 統一のメモを追記し改行ルール適用。ワークスペースを web-app に変更し .cursor を web-app に統一
- 親習慣インデントずれのバグ解析を一旦中止: 日誌の習慣リストで「親習慣のテキストがチェック付き行より右にずれて見える」件について Debug モードで計測（getBoundingClientRect で列・名前左端を送信）。ログでは親/通常で nameStartDiff・nameInnerDiff は 0 で一致していたが視覚的ずれが残るため解析を保留。計装（ref・useEffect・data-debug-*）は削除してコミット

#### 決定事項

- 特になし

#### 次回予定

- 一括生成: 「AI判定を実行」押下で「再生成」に変える／再生成の1日上限を2回に

- 習慣: チェック時の増減ゴルド数・EXP数の表示／並び順の統一／Complete・SuperComplete ボーナス実装

- 子習慣ネスト機能の実装（DB・API・スコア・UI）

- 以下のBuild（実装作業）を行う。
`.cursor/plans/子習慣ネスト機能_076a37a2.plan.md`

- 手動テストの続きを行う

---

### 260210-火

#### 実施内容

- 表示名まわり: 個別あらすじAPIで getStorySystemMessage に nickname を渡すよう修正。profile API の username と use_username_as_display_name を1回の UPDATE に統合。設定画面のプロファイル取得にエラーハンドリングと username のみフォールバックを追加（リロード時空欄になる問題を解消）。batch / story / advice 各APIのプロファイル取得にも同様のフォールバックを追加。調査ドキュメントに修正履歴を追記
- Google Auth: 新規ユーザー登録時の「Database error saving new user」対策として fix-trigger-handle-new-user.sql を追加（handle_new_user で username のフォールバック・デフォルト習慣失敗時も profiles は作成するよう EXCEPTION で囲む）。appendix に原因・対処ドキュメントを追加
- ログアウト後の Google 再ログインで PKCE code verifier エラーが出る問題を解消: OAuth 開始をサーバー側に移し GET /api/auth/google を新設。ログイン・サインアップの「Googleでログイン」「Googleで登録」をこの API へのリンクに変更。appendix にドキュメントを追加・更新

#### 決定事項

- 特になし

#### 次回予定

- 未定

---

### 260207-土

#### 実施内容

- 習慣・日誌: 過去日付でも習慣チェック・日誌が書けるよう、選択日付に daily_log がなければ作成（今日に限らず）
- 習慣: dailyLogId が null のときトーストで「この日付の日誌がまだありません」と表示
- AI一括生成: 4xx/5xx 時に実際のエラー文言を表示（Response を throw する場合のハンドリング）、カラム未追加時は「AI一括生成用のDB更新が必要です」を返す
- 世界観: 「蝦夷」を出さないよう北の大地・北国に変更、プロンプトに禁止指示を追加。表示名はバッチAPIでサーバー側 profiles のみ参照
- 設定: 管理者用「世界観の詳細設定」を isAdmin 時は常に表示（取得中・失敗時は読み込み中/再読み込み）。NEXT_PUBLIC_ADMIN_EMAILS で管理者メールを指定可能に（page + lib/auth/admin）
- 日誌: 利用数ポイント表示の括弧を削除（-1G のみ表示）。「あらすじ：これからの冒険」の見出し行をプロンプト禁止＋表示時除去。彼/彼女禁止をプロンプト・システムメッセージに追加。表示名・性別中立のプロンプト強化
- 再生成: 一括生成直後に「これからの冒険」が上書きされないよう skipNextAiStorySyncRef で保護
- 管理者: 再生成回数リセットAPI（POST /api/ai/batch-reset）と、管理者・テストアカウントのみ表示の「再生成回数をリセット（管理者用）」ボタンを追加。isAdmin をダッシュボード→JournalForm に渡す
- 表示名調査: バッチAPIで開発時ログ・_debug_nickname_used を返却、成功トーストに表示名を表示。docs/appendix/表示名がデフォルトになる原因調査.md と 7 にメモを追記

#### 決定事項

- 特になし

#### 次回予定

- 未定

---

### 260206-金

#### 実施内容

- ToDoサマリー: 画面上に1行説明を追加（「この画面ではタスクのドラッグ操作や状態変更はできません。状態の変更は、日誌タブのカンバンで行います。ここでは編集・削除のみできます。」）、見出し「全ToDoリスト一覧」直下に直列配置・白文字・上マージン15px
- ToDoサマリー: カードのドラッグを無効化（useDraggable を disabled: true にし、チェック操作ができないようサブタスクのチェックボックスのみ非表示（サブタスク名・編集・削除は表示のまま）
- 2-support-of-progress: 手動テスト対応依頼箇所のうち「→ **対応:**」がある項目の見出しに【対応済み】と（対応日: 要記入）を追加

#### 決定事項

- 特になし

#### 次回予定

- 未定

---

### 260205-木

#### 実施内容

- 手動テスト対応: 日誌の新規タスクボタン（ToDoリストフィルター枠内右上）、日誌カンバンは状態変更のみの仕様明文化
- ToDoサマリー: 表示期間「YYYY年MM月」のみ、新規時の属性（やさしい）で切り替え可能に、期限に曜日表示、サブタスク入力横幅いっぱい
- 辛口コーチング: アドバイスにニックネーム反映（APIで profiles 取得・プロンプトで「〇〇よ。」指示）
- 設定: 「この名前をGOL世界の表示名として利用する」チェックボックス追加（profiles.use_username_as_display_name、API・アドバイス/ストーリーで参照）
- 07: 手動テスト対応依頼箇所への「対応」追記、実施内容まとめセクション追加

#### 決定事項

- 特になし

#### 次回予定

- 未実装の要件検討（習慣: AIポイントおまかせ・Completeボーナス・利用ゴルド表示、アドバイス: 一括生成・再生成1日1回制限）

---

### 260204-水

#### 実施内容

- エクスポート機能（CSV/JSON）: GET /api/user/export、lib/export-csv.ts、マイページ各種設定グリッドに「データのエクスポート」カード（JSON/CSVボタン）を追加
- 進捗: 統計・分析の「エクスポート機能」完了、インポート機能をサブタスクとして追加
- マイページ: アバター幅をrem指定（中サイズで450px相当・文字サイズ連動）、フォント中サイズ17px、次のレベルアップまで・身体頭脳精神の文字サイズを2段階拡大、エクスポートを設定カードとして表示
- ダッシュボード: 日付選択（DateSelector）をヘッダーからタブ右端へ移動
- パフォーマンス最適化の親項目を完了に更新（子タスクは既に完了済み）

#### 決定事項

- 特になし

#### 次回予定

- 未定（インポート機能・E2Eテスト・デプロイ準備など）

---

### 260203-火

#### 実施内容

**ランク・アバター対応:**
- ランクシステム（Lv1–10、ヨウテイ/ドラクエモード別ランク名、EXP閾値）の実装
- RankAvatar コンポーネント追加（モード別・icon/full、png→jpg→svg フォールバック）
- アバター画像の命名・配置: ghost アイコンは i-yo1～i-yo10、全身は yo1～yo10。dq は dq1-i, dq1 形式。avatars/{ghost|dq}/{icon|full}/ に配置
- 日誌画面: アバター160px、固形背景・角丸R小さめ、Lv｜ランク名｜ゴルドを名前の下に配置
- マイページ: アバターをテキスト右側に配置、全身画像600px、レスポンシブ対応
- syncProfileLevel、rank_change_logs テーブル、avatar.md 仕様書の整備

#### 決定事項

- アバター形式: PNG（透過可）を推奨、容量目安はアイコン50–80KB・全身100–150KB
- 日誌画面アバター: 固形背景（bg-zinc-800）＋角丸（rounded=4px）

#### 次回予定

- （未定）

---


### 260202-月

#### 実施内容

**Google OAuth（Supabase Auth）の安定化:**
- Supabase で Google プロバイダを有効化し、クライアントID・シークレット・承認済みリダイレクト URI を設定
- PKCE の code_verifier をクッキーで共有するため、共通 cookieOptions（lib/supabase/cookie-options.ts）を用意し、ブラウザ・サーバー両方の Supabase クライアントに適用
- 認証コールバックを Route Handler（/auth/callback）で実装: exchangeCodeForSession 後、setAll が呼ばれるまで待ってからセッション用クッキーを付与して /auth/success へリダイレクト
- 中間ページ /auth/success を経由してからダッシュボードへ遷移（セッションクッキーが確実に送られるようにする）
- ログイン・サインアップで queryParams: { prompt: 'select_account' } を指定し、毎回 Google のアカウント選択を表示
- signInWithOAuth の戻り値 data.url で手動リダイレクトし、300ms 待ってから遷移（code_verifier をクッキーに書き込んでから Google へ飛ぶ）
- 07 に Google クライアント ID の表示手順、redirect_uri_mismatch の対処、PKCE/コールバックの対処を追記

#### 決定事項

- コールバック先は /auth/callback（Route Handler）。成功時は /auth/success を経由してダッシュボードへ
- 通常ブラウザでログインできない場合は localhost の sb-auth-token 系クッキーを削除してから再試行

#### 次回予定

- （未定）

---


### 260201-日

#### 実施内容

**AI作成文章・表示:**
- 日誌・一言感想のテキストエリアのフォントサイズを1px上げる（journal-impression-sections.tsx）
- AI作成文章の改行ルール（。？（全角）・?（半角）の後は必ず改行）を02に記載し、あらすじ・辛口コーチングアドバイス表示に applyAiTextLineBreaks を適用
- ユーザー名をボールド表示するルールを02に記載し、あらすじ・アドバイス表示に renderAiText（userName）を適用

**メールアドレス変更:**
- 設定画面（アカウント）にメールアドレス（ログイン用）変更セクションを追加（updateUser + 確認メール案内）
- 02に Supabase Auth メール変更フロー・トラブルシュート・リダイレクト・端末の注意を追記
- メールが届いただけでは変更されない旨を設定画面と02に明記

**02・07の棲み分け:**
- 02は確定事項の仕様のみ、07は未確定・確認事項を書くルールを02・07の冒頭と棲み分けセクションに明記
- 進行中の確認用メモの置き場を 2-support-of-progress.md に統一（2内の10→7参照に修正）

**世界観2種類（案C）:**
- 02の将来の拡張機能に「案C: コードで2種類固定の実装手順」を追記

**UI:**
- 過去の日誌一覧から日誌本文・一言感想のプレビュー表示を削除（日付・確定済みバッジのみ）
- 日付選択UIを prev/next 矢印と日付入力欄をやめ、カレンダーアイコンのみに変更（クリックでカレンダーダイアログを開く）
- ダッシュボード左上の日付表示の色を白（text-white）に変更

#### 次回予定

- （未定）※Phase 5 残、Phase 6、世界観2種類の実装、要望の洗い出しなどから選択可能

---


## 2601 --------------

### 260131-土

#### 実施内容

**権利設定画面:**
- 設定数 X/24 を昇順・降順と同じ行に配置、文言を「現在の設定数」に変更
- ディスクリプションを「権利の追加・編集・削除ができます。権利記号はアルファベットのみ。」に簡略化
- 仕様単位・使用条件 → 使用単位・条件 に表記変更

**本日の利用ゴルド:**
- 権利設定画面へのリンクをカード下部に追加（右寄せ、Settingsアイコン）

**日誌確定機能:**
- daily_logs に is_confirmed カラムを追加（SQLスクリプト作成）
- 確定ボタン・確定取り消しボタンを実装（取り消しは当日のみ）
- 未確定の過去日誌は編集可能、確定済みの過去日誌は編集不可に変更
- 日誌一覧に確定済みバッジを表示
- 編集可否メッセージをページ上部（タブ上）に移動

**過去の日誌・日付選択:**
- Next.js 15 対応: searchParams を await するように修正
- 過去の日誌クリックで左上の日付が正しく変わるよう修正
- 日誌リンクのナビゲーションを window.location.href でフルリロードに統一

**習慣作成エラー対応:**
- habits に difficulty カラムがない場合のエラーを解消（insert/update から difficulty 削除、Habit 型で difficulty をオプショナルに、add-difficulty-to-habits.sql 追加）

**日誌確定・取り消し:**
- 確定・取り消しを全日誌で可能に（当日のみ制限を解除）
- 「確定を取り消す」ボタンのスタイルをグレー背景＋白文字に変更

**AIあらすじ・アドバイス:**
- あらすじと辛口コーチングアドバイスの表示順を入れ替え（あらすじを上に）
- あらすじを「これまでの冒険」「これからの冒険」の2ブロックに分割、セクションラベルを「あらすじ」に
- あらすじの主人公名をユーザーのニックネーム（profiles.username）で生成するようプロンプト・APIを修正
- あらすじの再生成対応（生成済み時は「再生成」ボタン表示）
- 「これまでの冒険」「これからの冒険」ラベルを白文字に、獲得ゴルド/EXPのフォントを text-base に

**appendix:**
- 07/08/09/11 の数字接頭辞を外し、大文字アルファベット名にリネーム（DATABASE_SCHEMA.md, MD_SYNC_ANALYSIS.md, SERVER_VALIDATION.md, TAGS_DIFFICULTY_DESIGN.md）

#### 次回予定

- （未定）

---

### 260130-金

#### 実施内容

**権利設定の拡張・UI改善:**
- 「表示コード」を「権利記号」に変更。カード見出しを「権利」＋記号入力にし、記号部分のみ直接編集可能に
- 権利記号の大文字自動変換、保存時バリデーション（空・重複・消費量1未満）、削除時確認ダイアログ
- 権利の上限を24件に拡張。`lib/rights.ts` で RIGHT_COLUMNS_BY_INDEX / MAX_RIGHTS を定義し、設定API・日誌APIで共有
- 保存時に権利記号の昇順でソート。並び替え・追加・保存ボタンのレイアウト・スタイル調整

**ダッシュボード・日誌:**
- ヘッダー左に日付を「YYYY年MM月DD日(W)」で表示。デフォルト日付と「今日」判定を日本時間（Asia/Tokyo）に変更（UTCずれを解消）
- 「本日の利用ゴルド」カード内のフォント色を明るく調整。日誌タブ内の重複日付表示を削除

**習慣リストモーダル:**
- 習慣の種類セレクトを削除（開いたボタンで種類を決定）。入力タイプのセレクトにカスタム矢印スタイルを適用

**設定・API:**
- アカウント設定ページとプロファイル取得API（`/api/user/profile`）を追加
- daily_logs の権利カラムを24件対応に（型・日誌API）。points の文字列受け取り対応、保存エラー時に詳細メッセージを返すように変更

**その他UI:**
- 完了済みタスクの取り消し線を一本線・太さ3pxに統一（二重線をやめ、Tailwindの`decoration-[3px]`。設定ToDo・ToDoサマリー・Kanbanの6箇所）

#### 次回予定

- （未定）

---

### 260129-木

#### 実施内容

**ToDoサマリー・カードUIの調整:**

- 超過表示: ラベル文言をやめ、⚠️アイコンのみ＋赤字で表示。アイコン右に余白（mr-1.5）を追加
- 削除操作: カード上の削除ボタンを廃止し、編集モーダル内にのみ「削除」ボタンを配置。削除はモーダルからのみ実行
- 編集ボタン: 難易度の下の行（報酬行）の右端に配置。報酬行を flex で左に報酬・右に編集に変更
- カードの視認性: 報酬ラベル・期限のテキストを白（text-white）に。ToDoタイトルをボールド（font-bold）に
- フォントサイズ: 報酬・期限の行を text-sm に変更（タイトルは text-base のまま）

#### 決定事項

- 削除は編集モーダル内でのみ実行する運用に統一
- 超過はアイコンのみ表示（ラベルなし）で統一

#### 次回予定

- 動作確認・必要に応じて細部調整

---

### 260128-水

#### 実施内容

**To Do 操作の動線整理・確認:**

- To Do 操作の現状整理と確認
  - 各操作がどの画面から実行可能かを整理・確認
  - 新規タスク追加: ToDoサマリータブのみ（「+ 新規タスク」ボタン）
  - ステータス変更: 日誌タブのカンバン（ドラッグ&ドロップ）またはToDoサマリー（編集モーダル内）
  - タスクの編集（名前・期限・報酬・難易度・タグ）: ToDoサマリータブのみ（「📝編集」ボタン）
  - タスクの削除: ToDoサマリータブのみ（「🗑削除」ボタン）
  - サブタスクの操作: ToDoサマリータブのみ（展開して追加・編集・削除・チェック）
- 画面の役割分担の明確化
  - 日誌タブ: その日のTo Doを「ドラッグでステータス変更」する画面（実行画面）
  - ToDoサマリータブ: To Doの「一覧・追加・編集・削除・サブタスク」を扱う画面（管理画面）

**使いやすい導線の検討:**

- 使い勝手改善のための導線検討
  - 課題: 日誌のカンバンで「今日のタスクを触りたい」ときに、名前や期限を直したくなったらサマリーへタブ切り替えが必要で、文脈が切れる
  - 案A（推奨）: 日誌のカードに「📝編集」リンクを追加 → クリックでToDoサマリータブへ切り替え＋該当タスクの編集モーダルを開く
    - 実装方法: `dashboard-tabs`で`activeTab`と「編集対象のtodoId」を共有し、サマリー側で`todoId`があればモーダルを開く
  - 案B: 日誌のカードから小さな編集モーダルをその場で開く（名前・期限くらいだけ）
    - サマリーでしかできない操作（削除・サブタスク・タグなど）は「サマリーで詳しく編集」リンクで誘導
- 「今日やること」の導線を短くする検討
  - カンバン見出し横に「+ タスクを追加」「一覧・編集はToDoサマリー」などのリンクを追加する案
  - 日誌タブのカンバン直上に短い説明「タスクの名前・期限・報酬の変更はToDoサマリーから」を表示する案
- 実装優先順位の決定
  - まずは案A（日誌のカードから「編集」→ ToDoサマリータブへ切り替え＋該当タスクの編集モーダルを開く）から着手することを推奨

**Git自動PUSH機能の実装（MD版と同様の仕組み）:**

- 自動コミット・プッシュスクリプトの作成
  - `general/git-auto-commit.sh`: PC起動時に自動実行されるスクリプト
  - 1日1回のみ実行（`.last-commit-date.md`でチェック）
  - 変更がある場合のみコミット・プッシュ（`gol-web/`と`docs/`を対象）
- セットアップスクリプトの作成
  - `general/setup-auto-commit.sh`: 新しいMacでも簡単にセットアップできるスクリプト
  - launchd設定ファイルの自動生成・配置
  - リポジトリパスとユーザー名を自動設定
- launchd設定テンプレートの作成
  - `general/git-auto-commit.plist.template`: テンプレートファイル
  - リポジトリに含めて、どのMacでも使えるように
- README更新
  - 自動コミットシステムの説明を追加
  - 新しいMacでのセットアップ手順を追加（セットアップスクリプト使用と手動セットアップの2通り）
- `.gitignore`更新
  - `general/.last-commit-date.md`を除外対象に追加

**作業記録:**

- `docs/2-support-of-progress.md`にTo Do操作の動線整理と使いやすい導線の検討内容を詳細に記載

#### 決定事項

- To Do操作の動線: 日誌タブは実行画面、ToDoサマリータブは管理画面として役割分担を明確化
- 使い勝手改善: 案A（日誌のカードから「編集」→ ToDoサマリータブへ切り替え＋該当タスクの編集モーダルを開く）を優先的に実装する方向で検討
- Git自動PUSH機能: MD版と同様の仕組みを実装し、新しいMacでも`./general/setup-auto-commit.sh`を実行するだけでセットアップ可能に

#### 成果物

**ドキュメント:**
- `docs/2-support-of-progress.md`: To Do操作の動線整理と使いやすい導線の検討内容を詳細に記載
- `README.md`: 自動コミットシステムの説明とセットアップ手順を追加

**コード:**
- `general/git-auto-commit.sh`: 自動コミット・プッシュスクリプト
- `general/setup-auto-commit.sh`: セットアップスクリプト
- `general/git-auto-commit.plist.template`: launchd設定テンプレート
- `.gitignore`: `.last-commit-date.md`を除外対象に追加

#### 学んだこと

- UI/UX設計における「その場で完結」の重要性（文脈が切れないようにする）
- 画面の役割分担を明確にすることで、ユーザーの操作フローが自然になる
- 実装優先順位の判断: 実装量と分かりやすさのバランスを考慮して案Aを推奨

#### 次回予定

- 動作確認（新規ToDo作成・日付切り替えでエラーが出ないか）
- 案Aの実装検討（日誌のカードから「編集」→ ToDoサマリータブへ切り替え＋該当タスクの編集モーダルを開く）
- 必要に応じて push

---

### 260127-火

#### 実施内容

**Hydration エラー・無限ループの解消:**

- カンバンボード（kanban-board.tsx）
  - フィルター・ソートの初期値をサーバーとクライアントで統一。localStorage の復元はマウント後の useEffect のみに変更
  - DndContext 配下は isClient が立つまでプレースホルダー表示にし、@dnd-kit の aria-* による Hydration エラーを回避
  - テキスト・日付表示に suppressHydrationWarning を付与
- ToDoサマリー（todo-summary-tab.tsx）
  - フィルターの初期値も同様に「初回デフォルト、復元は useEffect」に変更
  - filteredActiveTodos / filteredInProgressTodos を useMemo でメモ化し、useEffect の無限ループ（Maximum update depth exceeded）を解消
- docs/0-AI-prompt-memo.md を .gitignore に追加し、追跡対象から除外（git rm --cached 済み）

**リモートリポジトリ作成・Git管理化:**

- gol-web-app のリモートリポジトリを Private で作成（GitHub 上で用意）
- 本プロジェクト（web-app）を Git 管理下に置いた（README・.gitignore 整備含む）
- md版（gol-md-app）と Web版（gol-web-app）は別リポジトリで管理する方針（同期しない）
- 作成手順（ブラウザ／CLI）は docs/0-AI-prompt-memo.md に記載

#### 決定事項

- カンバン・ToDoサマリーの localStorage 利用は「初回はデフォルト、マウント後に useEffect で復元」に統一して Hydration エラーを防ぐ
- gol/web-app への git 操作は working_directory を明示して実行する（マルチルートワークスペースで反映させるため）

#### 次回予定

- 動作確認（新規ToDo作成・日付切り替えでエラーが出ないか）
- 必要に応じて push

---

### 260123-金

#### 実施内容

**過去の日誌表示機能の改善:**

- 過去の日誌表示を2つのセクションに分割
  - 「今月の日誌」セクション: 今月の日誌のみを表示（件数表示付き）
  - 「過去の日誌」セクション: 今月以外の日誌をすべて表示（件数表示付き）
- 各セクションに降順・昇順の並び替え機能を追加
  - 並び替えボタンで降順⇔昇順を切り替え可能
  - アイコンで現在の並び順を表示（↓降順、↑昇順）
- 各セクションを独立して開閉可能（アコーディオン機能）
- パフォーマンス対策: `max-h-96`でスクロール可能に制限、`useMemo`でフィルタリングと並び替えを最適化

**AIアドバイス機能の名称変更:**

- 「厳しめコーチングアドバイス」→「辛口コーチング アドバイス」に名称変更
  - UI表示（journal-form.tsx）の見出しとaria-labelを更新
  - API Route（app/api/ai/advice/route.ts）のコメントとシステムメッセージを更新
  - プロンプト生成関数（lib/ai/openai.ts）の文言を更新

**ToDoリストの並び替え機能の実装:**

- 期限表示の変更
  - 形式を`YYMMDD-W`から`YY/MM/DD`に変更（例：`26/01/23`）
  - 西暦の下二桁を含む形式に統一
- 並び替えロジックの実装
  - 期限超過のToDoを一番上に固定表示
  - 期限超過以外は期限の古い順（昇順）に並び替え
  - 期限がないToDoは後ろに配置
- ドラッグ&ドロップ機能の実装
  - `@dnd-kit/core`を使用してドラッグ&ドロップ機能を実装
  - 期限超過のToDoはドラッグ不可（一番上に固定）
  - 期限超過以外のToDoは自由に並び替え可能
  - 並び替え後は`display_order`をデータベースに保存
  - ドラッグ中の視覚的フィードバック（オーバーレイ表示）

**バグ修正:**

- `isOverdue`関数の定義順序を修正（`sortTodos`より前に移動）
  - 「Cannot access 'isOverdue' before initialization」エラーを解消

#### 決定事項

- 過去の日誌表示: 今月の日誌と過去の日誌を分けて管理し、各セクションで独立して並び替え可能
- AIアドバイス機能: 「辛口コーチング アドバイス」という名称に統一
- ToDoリストの並び替え: 期限超過を一番上に固定、それ以外は期限の昇順で並び替え、ドラッグ&ドロップで自由に並び替え可能

#### 成果物

- `components/journal-list.tsx`: 2つのセクションに分割、並び替え機能追加
- `app/dashboard/journal-form.tsx`: AIアドバイス機能の名称変更
- `app/api/ai/advice/route.ts`: コメントとシステムメッセージの更新
- `lib/ai/openai.ts`: プロンプト生成関数の文言更新
- `app/dashboard/todo-summary-tab.tsx`: 期限表示の変更、並び替えロジック、ドラッグ&ドロップ機能の実装

#### 次回予定

- 動作確認（すべての機能が正しく動作することを確認）
- ToDoリストの並び替え機能の動作確認（ドラッグ&ドロップが正しく動作することを確認）

---

### 260122-木

#### 実施内容

**タグ・フィルター・難易度設定機能の実装完了:**

**悪習慣のポイント処理変更:**

- Phase 1: データベーススキーマの追加（tags, habit_tags, todo_tagsテーブル、difficultyカラム追加）
- Phase 1: 型定義の追加（Tag, HabitTag, TodoTag, Difficulty型）
- Phase 2: 難易度設定UIの実装（習慣・ToDo編集モーダル）
- Phase 2: 難易度表示UIの実装（習慣リスト・ToDoカンバン）
- Phase 3: タグ管理APIの実装（CRUD操作）
- Phase 3: タグ関連付けUIの実装
- Phase 4: フィルター機能の実装（タグ・難易度によるフィルタリング）
- 追加実装: タグ管理UI（タグ作成・編集・削除機能）
- 追加実装: フィルター状態のローカルストレージ保存機能
- 追加実装: フィルター適用時の結果件数表示機能
- 追加実装: 難易度に応じた報酬調整機能（難易度倍率システム）

#### 決定事項

- 難易度倍率の設定:
  - trivial: 0.5x（簡単なタスクは報酬が少ない）
  - easy: 0.75x
  - medium: 1.0x（基準）
  - hard: 1.5x（難しいタスクは報酬が多い）
- フィルター状態はローカルストレージに保存し、ページリロード後も保持
- タグ管理は習慣・ToDo編集モーダル内から直接アクセス可能

#### 成果物

- SQLファイル: `supabase-tags-difficulty-setup.sql`
- API Route: `/api/tags`, `/api/tags/[id]`, `/api/habits/[habitId]/tags`, `/api/todos/[todoId]/tags`
- UI更新: 習慣リスト、ToDoカンバン、ToDoサマリータブにフィルターUI追加
- タグ管理モーダル: 習慣・ToDo編集モーダル内からタグの作成・編集・削除が可能
- 難易度倍率システム: ToDo完了時の報酬が難易度に応じて自動調整

**悪習慣のポイント処理変更:**

- 悪習慣のポイント処理ロジックを変更
  - 従来: 悪習慣を回避したら（チェックを入れなかったら）ポイントがプラス
  - 変更後: 悪習慣をやってしまったら（チェックを入れたら）ポイントがマイナス、チェックを外したら（回避したら）プラス（元に戻す）
- 悪習慣セクションに「+ 悪習慣を追加」ボタンを追加
- ボタンラベルを「+ 良習慣を追加」「+ 悪習慣を追加」に変更
- 悪習慣のポイント表示をマイナス表示（赤色）に変更
- トランザクション整合性の改善（プロファイル更新を先に実行し、失敗時はhabit_logsを更新しない）

#### 決定事項

- 悪習慣のポイント処理:
  - チェックON（実行）→ ポイント/EXPをマイナス
  - チェックOFF（回避）→ ポイント/EXPをプラス（元に戻す）
- 良習慣は従来通り、チェックON（実行）→ ポイント/EXPをプラス

**月ごとのToDo管理機能の実装:**

- ToDoサマリー画面に月ごとのフィルター機能を追加
  - セレクトBOXで「すべてのToDo」または「YYYY年MM月のToDo」を選択可能
  - 完了済みToDoを`completed_at`を基準に月ごとにグループ化
  - 月ごとのオプションリストを自動生成（降順：最新の月が先頭）
  - フィルターリセットボタンに月フィルターのクリア機能を追加
- ToDoサマリー画面のレイアウトを3列カラムに変更
  - 日誌画面（Kanban Board）と同じ3列カラムレイアウトに変更（アクティブ・進行中・完了済み）
  - ToDoカード表示用の共通関数`renderTodoCard`を作成してコードの重複を削減
  - モバイルでは1列、デスクトップでは3列表示（レスポンシブ対応）
- Kanban Board（日誌画面）の完了済みカラムを今月のものだけにフィルタリング
  - 今月の開始日と終了日を計算して、`completed_at`が今月の範囲内のToDoのみ表示
  - アクティブ・進行中カラムはすべてのToDoを表示（変更なし）
- 構文エラーの修正
  - `renderTodoCard`関数を`return`文の前に移動
  - 不要なコードの削除（重複したButton要素など）

#### 決定事項

- 難易度倍率の設定:
  - trivial: 0.5x（簡単なタスクは報酬が少ない）
  - easy: 0.75x
  - medium: 1.0x（基準）
  - hard: 1.5x（難しいタスクは報酬が多い）
- フィルター状態はローカルストレージに保存し、ページリロード後も保持
- タグ管理は習慣・ToDo編集モーダル内から直接アクセス可能
- 月ごとのToDo管理:
  - ToDoサマリー画面: すべてのToDoを表示、または特定の月のToDoのみを表示（月フィルター適用）
  - Kanban Board: 完了済みカラムは今月完了のToDoのみ表示（月が変わると自動で更新）
- ToDoサマリー画面のレイアウト: 日誌画面と同じ3列カラムレイアウトで統一

#### 成果物

- SQLファイル: `supabase-tags-difficulty-setup.sql`
- API Route: `/api/tags`, `/api/tags/[id]`, `/api/habits/[habitId]/tags`, `/api/todos/[todoId]/tags`
- UI更新: 習慣リスト、ToDoカンバン、ToDoサマリータブにフィルターUI追加
- タグ管理モーダル: 習慣・ToDo編集モーダル内からタグの作成・編集・削除が可能
- 難易度倍率システム: ToDo完了時の報酬が難易度に応じて自動調整
- **ToDoサマリー画面**: 3列カラムレイアウト、月ごとのフィルター機能
- **Kanban Board**: 完了済みカラムを今月のものだけにフィルタリング

#### 次回予定

- 動作確認（すべての機能が正しく動作することを確認）
- 月ごとのToDo管理機能の動作確認（月フィルターが正しく動作することを確認）
- 3列カラムレイアウトの動作確認（レスポンシブ対応が正しく動作することを確認）

---

### 260119-月

#### 実施内容

**マークダウン同期機能実装に向けた検討（継続）:**

**機能的な違いの詳細分析の続き:**
- `MD_WEB_SYNC_IMPLEMENTATION_GUIDE.md`に詳細な解決策を追加
- 習慣名のマッチング: 完全一致→基本名一致→正規化後一致→部分一致の優先順位を明確化
  - 複数選択肢がある場合（例: "懸垂｜10回 or ディップス｜10回"）の対応方法を検討
  - 特殊文字が含まれる場合（例: "起床後 or 起床 → 運動後すぐの冷水シャワー"）の対応方法を検討
- 数値入力習慣の扱い: 複数パターンに対応する抽出ロジックを実装
  - 「数字+単位」形式（例: "10回", "30秒"）
  - 「最大Nまで」形式
  - 「Nごとに」形式
- EXP配分の同期: ToDo完了時とAI判定結果の両方に対応するパース・フォーマット関数を実装
  - ToDo完了時: `身体: +0 / 頭脳: +1 / 精神: +1`形式
  - AI判定結果: `身体: +4｜現在の累積 481`形式

**データ移行の実施を試行:**
- `right_e_count`カラムをデータベースに追加（`supabase-add-right-e.sql`を実行）
- 移行スクリプト（`migrate-md-to-web.ts`）の実行を試行
- データ抽出の正規表現パターンに問題があることを確認
- MD版とWeb版のデータ形状の違いを確認
  - マークダウンファイルには「処理済み/最新話」と「処理前/ドラフト/当日日誌」の2つのセクションが存在
  - 正規表現パターンが実際の構造と一致していない

**決定事項:**
- データ移行は一旦保留（マークダウンファイルの構造が複雑で、正規表現パターンの修正に時間がかかるため）
- 同期実装の方針決定を優先して進める

**次回予定:**
- 同期実装の方針決定
  - ファイルアクセス方法の決定（GitHub API / Supabase Storage / ローカル）
  - 同期方向の決定（MD → Web / Web → MD / 双方向）
  - 競合解決方法の決定
- 実装順序の確定

---

### 260118-日

#### 実施内容

**マークダウン同期機能実装に向けた準備作業:**

**補助資料の作成:**
- `gol/MD_WEB_SYNC_IMPLEMENTATION_GUIDE.md`: マークダウン版とウェブ版の同期機能実装に向けた補助資料を作成
  - ディレクトリ構成の違いの分析
  - 機能的な違いの詳細分析（日誌データ、習慣データ、ToDoタスクデータ、権利データ、AI判定結果）
  - 同期実装の方針と注意点
  - 実装順序の推奨

**ディレクトリ構成の違いについての検討:**
- MD版とWeb版のディレクトリ構成を比較・分析
- 検討事項の整理:
  - ファイルアクセス方法（GitHub API / Supabase Storage / ローカル）
  - データ構造の対応（分析完了）
  - 日付フォーマット変換（`YYMMDD-W` ↔ `YYYY-MM-DD`）
  - ファイル構造の統一（現状維持を推奨）
- `0-AI-prompt-memo.md`に検討内容を記載

**決定事項:**
- ファイル構造は現状維持（各版の役割が明確で、同期機能はAPI/ファイルパスを抽象化すれば対応可能）
- `docs/`ディレクトリのパス確認と修正（MD版: `md-app/docs/`、Web版: `web-app/docs/`）

**次回予定:**
- 機能的な違いの詳細分析の続き
- 同期実装の方針決定
- 実装順序の確定

---

### 260117-土

#### 実施内容

**パフォーマンス最適化の実装:**

**データ取得の最適化:**
- `app/dashboard/stats-tab.tsx`: 3つのAPI（points-exp, habits-completion, todos-completion）を並列取得に変更（Promise.all使用）
- `app/dashboard/page.tsx`: データベースクエリを並列化（habitsとtodosを同時取得、habit_logsとtodo_logsを同時取得）

**コンポーネントのメモ化:**
- `app/dashboard/stats-tab.tsx`: 
  - `fetchData`関数を`useCallback`でメモ化
  - `getHabitTypeColor`関数を`useCallback`でメモ化
  - 習慣の種類別データ分類を`useMemo`でメモ化
  - 統計サマリー計算を`useMemo`でメモ化
- `app/dashboard/habit-list.tsx`: `React.memo`でラップ（propsが変わったときだけ再レンダリング）
- `app/dashboard/kanban-board.tsx`: `React.memo`でラップ
- `app/dashboard/journal-form.tsx`: `React.memo`でラップ

**コード分割・動的インポート:**
- `app/dashboard/dashboard-tabs.tsx`: 統計タブ（StatsTab）を`lazy`と`Suspense`で動的インポートに変更
  - 統計タブが使用されない場合はバンドルサイズを削減
  - ローディング状態を表示

**UI改善・アコーディオン機能の実装:**

**アコーディオン機能の追加:**
- 全てのセクションにアコーディオン（折りたたみ/展開）機能を追加
  - `app/dashboard/kanban-board.tsx`: ToDoカンバンボードにアコーディオン機能を追加
  - `app/dashboard/habit-list.tsx`: 習慣チェックリスト（良習慣/悪習慣/ボーナス）にアコーディオン機能を追加
  - `app/dashboard/journal-form.tsx`: 日誌フォーム（今日の日誌、一言感想、本日の利用ポイント、AI判定）にアコーディオン機能を追加
  - `components/journal-list.tsx`: 過去の日誌一覧にアコーディオン機能を追加
- `lucide-react`の`ChevronDown`/`ChevronUp`アイコンを使用して視覚的に表示

**アコーディオン一括制御機能:**
- `app/dashboard/dashboard-tabs.tsx`: 画面全体で「全て開く」「全て閉じる」ボタンを1組のみに統一
  - 全てのアコーディオン状態を親コンポーネントで一括管理
  - 各コンポーネント（KanbanBoard、JournalForm、JournalList）にpropsで状態を渡すように変更
  - `types.ts`: 各コンポーネントのProps型定義に`isExpanded`/`expandedStates`と`onExpandedChange`/`onExpandedStateChange`を追加

**レイアウト調整:**
- 「今日の日誌」と「一言感想」をToDoカンバンボードの下に移動
  - `app/dashboard/journal-impression-sections.tsx`: 新規コンポーネントを作成（JournalFormから分離）
  - `app/dashboard/dashboard-tabs.tsx`: KanbanBoardの下にJournalImpressionSectionsを配置
- 「今日の日誌」と「一言感想」を横並びカラムレイアウトに変更
  - `grid grid-cols-1 lg:grid-cols-2`を使用して横並びレイアウト（デスクトップで2カラム、モバイルで1カラム）
- Textareaのデフォルト高さを600pxに設定
  - 両方のTextareaに`h-[600px]`と`overflow-y-auto`を設定
  - 内容が高さを超えた場合にスクロールを表示

#### 決定事項

- データ取得は並列化を優先（Promise.allを使用）
- 大きなコンポーネントは`React.memo`でメモ化
- 計算処理は`useMemo`でメモ化
- 関数は`useCallback`でメモ化
- 大きなコンポーネント（統計タブなど）は動的インポートでコード分割
- 全てのセクションにアコーディオン機能を追加して、画面を整理しやすくする
- アコーディオン一括制御は画面全体で1つのボタンで統一（各セクションごとではなく）
- 「今日の日誌」と「一言感想」はToDoカンバンボードの下に配置し、横並びカラムレイアウトにする
- Textareaのデフォルト高さは600pxとし、内容がそれを超えた場合にスクロールを表示する

#### 成果物

**コード:**
- `app/dashboard/stats-tab.tsx`: データ取得の並列化、メモ化の実装
- `app/dashboard/page.tsx`: データベースクエリの並列化
- `app/dashboard/dashboard-tabs.tsx`: 統計タブの動的インポート、アコーディオン一括制御
- `app/dashboard/habit-list.tsx`: React.memoでラップ、アコーディオン機能追加
- `app/dashboard/kanban-board.tsx`: React.memoでラップ、アコーディオン機能追加
- `app/dashboard/journal-form.tsx`: React.memoでラップ、アコーディオン機能追加
- `app/dashboard/journal-impression-sections.tsx`: 新規コンポーネント作成（今日の日誌と一言感想を分離）
- `components/journal-list.tsx`: アコーディオン機能追加
- `lib/types.ts`: 各コンポーネントのProps型定義にアコーディオン制御用の型を追加

#### 学んだこと

- Promise.allを使用した並列データ取得によるパフォーマンス向上
- React.memo、useMemo、useCallbackの適切な使い分け
- Next.jsの動的インポート（lazy、Suspense）によるコード分割
- データベースクエリの並列化による読み込み時間の短縮
- Reactコンポーネントの状態を親から子へpropsで渡す方法（外部制御パターン）
- コンポーネント分割によるレイアウトの柔軟な配置（JournalFormからJournalImpressionSectionsを分離）
- CSS Grid（`grid-cols-1 lg:grid-cols-2`）を使用したレスポンシブなカラムレイアウト

#### 次回予定

**UI改善の続き:**
- スマホ対応の実装（横並びカラムをモバイルで1カラムに変更済み、追加の最適化が必要か確認）

**パフォーマンス最適化の続き:**
- 画像・アセットの最適化（Next.js Imageコンポーネントの使用）

**その他のPhase 5タスク:**
- 統計・分析機能の続き（エクスポート機能）
- テスト実装の続き（E2Eテスト、データベース操作のテスト）
- MD版との同期機能実装
- デプロイ準備

### 260116-金

#### 実施内容

**統計・分析機能の実装開始 - ポイント・EXPの推移グラフ:**

- rechartsライブラリのインストール
  - `npm install recharts`でグラフ描画ライブラリをインストール
- 過去のデータ取得API Route作成
  - `app/api/stats/points-exp/route.ts`: 過去7日間・30日間・90日間のdaily_logsデータを取得
  - 認証チェック、期間パラメータ対応、データ整形（nullを0に変換）
- 統計タブコンポーネント作成
  - `app/dashboard/stats-tab.tsx`: ポイント・EXPの推移を表示する折れ線グラフコンポーネント
  - 期間選択（7日/30日/90日）、ローディング状態、エラーハンドリング
  - 4系列の折れ線グラフ（ポイント、身体EXP、頭脳EXP、精神EXP）
  - 統計サマリーカード（合計値表示）
- ダッシュボードに統計タブを追加
  - `dashboard-tabs.tsx`に「📊 統計」タブを追加
  - タブ切り替え機能を実装

**統計・分析機能の続き - 習慣の達成率グラフ:**

- 習慣の達成率データ取得API Route作成
  - `app/api/stats/habits-completion/route.ts`: 過去7日間・30日間・90日間のhabit_logsデータを取得
  - 習慣ごとの達成率計算（checkboxタイプ: is_checked、numberタイプ: count > 0）
  - 習慣の種類別（良習慣/悪習慣/ボーナス）の平均達成率を計算
- 統計タブに習慣の達成率グラフを追加
  - `app/dashboard/stats-tab.tsx`: 習慣の達成率を表示する横棒グラフコンポーネント
  - 習慣の種類別平均達成率サマリーカード（良習慣/悪習慣/ボーナス）
  - 習慣ごとの達成率を横棒グラフで表示（色分け: 良習慣=緑、悪習慣=赤、ボーナス=黄）
  - ツールチップで達成日数/総日数を表示

**統計・分析機能の続き - ToDo完了率の可視化:**

- ToDo完了率データ取得API Route作成
  - `app/api/stats/todos-completion/route.ts`: 過去7日間・30日間・90日間のtodosデータを取得
  - 完了済みタスク数/総タスク数で完了率を計算
  - SPタスク/通常タスク別の完了率を計算
  - 日別の完了率を計算（日付ごとの完了率推移）
- 統計タブにToDo完了率グラフを追加
  - `app/dashboard/stats-tab.tsx`: ToDo完了率を表示する折れ線グラフコンポーネント
  - サマリーカード（全体/SPタスク/通常タスクの完了率）
  - 日別完了率の折れ線グラフ（期間内の完了率推移を表示）
  - ツールチップで完了数/総数を表示

**統計・分析機能の続き - 進捗ダッシュボードの統合:**

- 週間・月間統計のサマリー表示機能実装
  - `app/dashboard/stats-tab.tsx`: 統計タブの先頭に週間・月間サマリーセクションを追加
  - 既存のAPI Route（points-exp, habits-completion, todos-completion）を活用
  - 週間（7日間）と月間（30日間）のデータを並列取得（Promise.all）
  - サマリー表示項目: ポイント、身体EXP、頭脳EXP、精神EXP、習慣達成率、ToDo完了率
  - 2カラムレイアウト（週間/月間を並列表示）
  - レスポンシブ対応（モバイルでは1カラム表示）

**テスト実装開始:**

- テストライブラリのインストールと設定
  - Vitest、React Testing Library、happy-domをインストール
  - `vitest.config.mjs`: Vitest設定ファイル作成（happy-dom環境、パスエイリアス設定）
  - `vitest.setup.ts`: テストセットアップファイル作成（@testing-library/jest-dom、Next.jsモック）
  - `package.json`: テストスクリプト追加（test, test:ui, test:coverage）
- 単体テストの実装
  - `lib/__tests__/utils.test.ts`: cnユーティリティ関数のテスト（クラス名マージ、条件付きクラス）
  - `lib/__tests__/validation.test.ts`: バリデーション関数のテスト（文字列長、数値範囲、列挙型など）
- コンポーネントテストの実装
  - `components/ui/__tests__/button.test.tsx`: Buttonコンポーネントのテスト（レンダリング、クリック、disabled、variant、size）
- API Routeのテスト実装
  - `app/api/stats/__tests__/points-exp.test.ts`: 統計API Routeのテスト（認証チェック、データ取得、null値処理）
- テスト実行結果: 20個のテストすべてパス ✅

#### 決定事項

- グラフライブラリ: rechartsを使用（軽量で使いやすい）
- 期間選択: 7日間・30日間・90日間から選択可能
- グラフ表示: 4系列を同時表示（ポイント、身体EXP、頭脳EXP、精神EXP）
- カラースキーム: ダークテーマに合わせた配色（シアン、緑、青、紫）

#### 成果物

**コード:**
- `app/api/stats/points-exp/route.ts`: 統計データ取得API Route（新規作成）
- `app/api/stats/habits-completion/route.ts`: 習慣の達成率データ取得API Route（新規作成）
- `app/api/stats/todos-completion/route.ts`: ToDo完了率データ取得API Route（新規作成）
- `app/dashboard/stats-tab.tsx`: 統計タブコンポーネント（ポイント・EXP推移グラフ、習慣の達成率グラフ、ToDo完了率グラフ、週間・月間サマリー）
- `app/dashboard/dashboard-tabs.tsx`: 統計タブを追加

**テスト:**
- `vitest.config.mjs`: Vitest設定ファイル
- `vitest.setup.ts`: テストセットアップファイル
- `lib/__tests__/utils.test.ts`: ユーティリティ関数のテスト
- `lib/__tests__/validation.test.ts`: バリデーション関数のテスト
- `components/ui/__tests__/button.test.tsx`: Buttonコンポーネントのテスト
- `app/api/stats/__tests__/points-exp.test.ts`: API Routeのテスト

**パッケージ:**
- `recharts`: グラフ描画ライブラリ
- `vitest`: テストフレームワーク
- `@testing-library/react`: Reactコンポーネントテストライブラリ
- `@testing-library/jest-dom`: DOMアサーション拡張
- `@testing-library/user-event`: ユーザーイベントシミュレーション
- `happy-dom`: 軽量なDOM実装（jsdomの代替）

#### 学んだこと

- rechartsの基本的な使い方（LineChart、ResponsiveContainer、Tooltip、Legend）
- ダークテーマ対応のグラフスタイリング
- 期間選択による動的データ取得
- 統計サマリーの集計表示
- Vitestの設定方法（happy-dom環境、パスエイリアス）
- React Testing Libraryの基本的な使い方（render, screen, userEvent）
- Next.js API Routeのテスト方法（Supabaseモック）
- テストセットアップファイルでのNext.jsモック（useRouter, usePathname）

#### 次回予定

**統計・分析機能の続き:**
- エクスポート機能（CSV/JSON）の実装

**その他のPhase 5タスク:**
- パフォーマンス最適化（データ取得の最適化、コンポーネントのメモ化）
- テスト実装（単体テスト、コンポーネントテスト、E2Eテスト）
- MD版との同期機能実装
- デプロイ準備

## 2512 --------------

### 251227-土（本日の作業サマリー）

**本日実装した内容:**

1. **ネットワークエラー時のリトライ機能・ローディング表示の改善**
   - リトライ機能のユーティリティ関数作成（指数バックオフ）
   - スケルトンUIの導入と適用
   - AI API呼び出しにリトライ機能を適用

2. **パスワードリセット機能の実装**
   - ログインページに「パスワードを忘れた場合」リンクを追加
   - パスワードリセット申請ページ（/forgot-password）の作成
   - 新しいパスワード設定ページ（/reset-password）の作成
   - Toast通知とセッション確認ロジックの改善

3. **サーバー側バリデーションの実装**
   - バリデーション用ユーティリティ関数の作成
   - AI API Routeへのバリデーション追加
   - データベース操作のAPI Route作成（daily-logs, habits, todos）
   - データベース制約との整合性確認
   - サーバー側バリデーション設計書の作成

**Phase 5の進捗:**
- エラーハンドリング・バリデーション強化: ✅ 完了

### 251227-土（続き）

#### 実施内容

**パスワードリセット機能の実装:**

- ログインページの修正
  - パスワード入力欄の右側に「パスワードを忘れた場合」リンクを追加
  - `/forgot-password`へのリンクを設置
- パスワードリセット申請ページの作成
  - `app/forgot-password/page.tsx`: メールアドレスを入力してリセットメールを送信
  - Supabaseの`resetPasswordForEmail`を使用
  - 成功時はメール送信完了メッセージを表示
- 新しいパスワード設定ページの作成
  - `app/reset-password/page.tsx`: メール内のリンクから遷移
  - 新しいパスワードとパスワード確認を入力
  - Supabaseの`updateUser`を使用してパスワードを更新
  - セッション確認: パスワードリセットトークンが有効か確認

#### 決定事項

- パスワードリセットフロー: メール送信 → メール内リンククリック → 新しいパスワード設定
- バリデーション: パスワード一致チェック、6文字以上チェック
- 自動リダイレクト: パスワード更新成功後、3秒後にログイン画面に自動リダイレクト

#### 成果物

**コード:**
- `app/login/page.tsx`: 「パスワードを忘れた場合」リンクを追加
- `app/forgot-password/page.tsx`: パスワードリセット申請ページ（新規作成）
- `app/reset-password/page.tsx`: 新しいパスワード設定ページ（新規作成）

#### 学んだこと

- Supabase認証: `resetPasswordForEmail`でパスワードリセットメールを送信
- パスワード更新: `updateUser`でパスワードを更新
- セッション管理: パスワードリセットトークンの有効性を確認
- UX設計: メール送信完了メッセージと自動リダイレクトでユーザー体験を向上
- Toast通知: 成功/エラー時のフィードバックを統一
- セッション確認: `useEffect`の依存配列を最適化して無限ループを防止

#### 次回予定

- Supabase Dashboardでの設定確認（Email Templates、URL Configuration）
- メールテンプレートのカスタマイズ（必要に応じて）

### 251227-土（続き2）

#### 実施内容

**Phase 5（エラーハンドリング・バリデーション強化）継続 - サーバー側バリデーションの実装:**

- バリデーション用ユーティリティ関数の作成
  - `lib/validation.ts`: 文字列、数値、列挙型、フォーマット、複合バリデーション関数を作成
  - データベース制約を反映（文字数制限、数値範囲、列挙型の値）
  - オプショナルフィールドの処理（null/undefinedを許可）
- AI API Routeへのバリデーション追加
  - `app/api/ai/judgment/route.ts`: 日誌本文・一言感想のバリデーション
  - `app/api/ai/advice/route.ts`: 日誌本文・一言感想・スコアのバリデーション
  - `app/api/ai/story/route.ts`: 日誌本文・一言感想のバリデーション
- データベース操作のAPI Route作成
  - `app/api/daily-logs/route.ts`: 日誌保存API（PUT）
  - `app/api/habits/route.ts`: 習慣管理API（POST, PUT, DELETE）
  - `app/api/todos/route.ts`: ToDo管理API（POST, PUT, DELETE）
  - すべてのAPI Routeで認証チェック、所有権チェック、データ存在確認を実装
- データベース制約との整合性確認
  - `docs/08-server-validation.md`: サーバー側バリデーション設計書を作成
  - データベース制約とバリデーションの対応表を作成

#### 決定事項

- バリデーション方針: クライアント側とサーバー側の二重チェック
- エラーレスポンス形式: `{ error: string, details?: string[] }`
- HTTPステータスコード: 400（バリデーションエラー）、401（認証エラー）、403（アクセス権限エラー）、404（リソースが見つからない）、500（サーバーエラー）

#### 成果物

**コード:**
- `lib/validation.ts`: バリデーション用ユーティリティ関数
- `app/api/ai/judgment/route.ts`: サーバー側バリデーション追加
- `app/api/ai/advice/route.ts`: サーバー側バリデーション追加
- `app/api/ai/story/route.ts`: サーバー側バリデーション追加
- `app/api/daily-logs/route.ts`: 日誌保存API Route（新規作成）
- `app/api/habits/route.ts`: 習慣管理API Route（新規作成）
- `app/api/todos/route.ts`: ToDo管理API Route（新規作成）

**ドキュメント:**
- `docs/08-server-validation.md`: サーバー側バリデーション設計書（新規作成）

#### 学んだこと

- 二重チェック: クライアント側とサーバー側の両方でバリデーションを行う重要性
- データベース制約: CHECK制約、UNIQUE制約、NOT NULL制約をバリデーションに反映
- 所有権チェック: データの所有権を確認してセキュリティを確保
- エラーハンドリング: 詳細なエラーメッセージを返してデバッグを容易に

#### 次回予定

- Client ComponentからAPI Routeを呼び出すように変更（オプション）
- habit_logs、todo_logs、todo_subtasksのAPI Route作成（必要に応じて）

### 251227-土

#### 実施内容

**Phase 5（エラーハンドリング・バリデーション強化）継続 - ネットワークエラー時のリトライ機能・ローディング表示の改善:**

- ネットワークエラー時のリトライ機能実装
  - `lib/api-retry.ts`: リトライ機能のユーティリティ関数を作成
    - `fetchWithRetry`関数: fetch APIをラップしてリトライ機能を提供
    - 指数バックオフアルゴリズム: 初期待機時間1000ms、最大待機時間10000ms
    - 最大リトライ回数: 3回（デフォルト）
    - ネットワークエラーの自動検出（TypeError、ECONNREFUSEDなど）
    - リトライ可能なHTTPステータスコードの判定（5xx、408）
  - `journal-form.tsx`: AI API呼び出しにリトライ機能を適用
    - `handleAIJudgment`: AI判定API呼び出し
    - `handleGenerateAdvice`: AIアドバイス生成API呼び出し
    - `handleGenerateStory`: AIあらすじ生成API呼び出し

- ローディング表示の改善
  - Skeletonコンポーネントの導入
    - `npx shadcn@latest add skeleton --yes`でインストール
    - ダークテーマに合わせてスタイルを調整（`bg-zinc-700`）
  - `journal-form.tsx`: ローディング中にスケルトンUIを表示
    - AI判定結果表示: スコア、理由、ポイント/EXPのスケルトン
    - AIアドバイス生成: 3行のテキストスケルトン
    - AIあらすじ生成: 4行のテキストスケルトン

- エラーハンドリングの改善
  - `Response`オブジェクトから詳細なエラー情報を抽出
  - HTTPステータスコードとステータステキストを表示
  - JSON形式のエラーレスポンスから`error`または`message`フィールドを取得

#### 決定事項

- リトライ機能: 指数バックオフで最大3回リトライ（初期待機時間1000ms、最大待機時間10000ms）
- スケルトンUI: shadcn/uiのSkeletonコンポーネントを使用、ダークテーマに合わせてスタイル調整
- エラーメッセージ: HTTPステータスコードと詳細情報を表示してユーザーに分かりやすく伝える

#### 成果物

**コード:**
- `lib/api-retry.ts`: リトライ機能のユーティリティ関数
- `components/ui/skeleton.tsx`: スケルトンUIコンポーネント（ダークテーマ対応）
- `app/dashboard/journal-form.tsx`: リトライ機能とスケルトンUIを適用

#### 学んだこと

- **指数バックオフ**: リトライ間隔を指数関数的に増やすことで、サーバーへの負荷を軽減
- **fetchWithRetry**: カスタムfetchラッパーでリトライロジックを統一
- **スケルトンUI**: ローディング中のユーザー体験を向上（コンテンツの形状を事前に表示）
- **エラーハンドリング**: `Response`オブジェクトから詳細なエラー情報を抽出する方法

#### 次回予定

**Phase 5 残りタスク:**
- パフォーマンス最適化（データ取得の最適化、コンポーネントのメモ化）
- 統計・分析機能の実装（進捗ダッシュボード、グラフ表示）
- テスト実装（単体テスト、コンポーネントテスト、E2Eテスト）
- MD版との同期機能実装
- デプロイ準備

### 251209-火（続き）

#### 実施内容

**Phase 3（機能拡張）継続 - 習慣の追加機能実装:**

- 習慣の追加機能実装
  - 「[+ 習慣を追加]」ボタンに機能追加
    - クリックでモーダルを開く機能を実装
  - 習慣追加モーダルの作成
    - ToDo追加モーダルと同様のデザインで実装
    - モーダルの開閉機能を実装
  - 習慣追加フォーム実装
    - 習慣名（必須）
    - 習慣の種類（良習慣/悪習慣/ボーナス）
    - 入力タイプ（チェックボックス/数値入力）
    - 報酬設定（ポイント、身体EXP、頭脳EXP、精神EXP）
    - オプション設定（週末除外、Completeボーナス対象外）
  - データベースへの保存機能実装
    - SupabaseのhabitsテーブルにINSERT処理
    - ユーザーID取得とdisplay_order自動計算
    - is_customフラグをtrueに設定（ユーザーが作成した習慣）
    - データ更新後のページリフレッシュ機能実装

#### 決定事項

- モーダル方式: ToDo追加と同様のモーダルUIで実装
- デフォルト値: 習慣の種類は「良習慣」、ポイントは1、入力タイプは「チェックボックス」
- カスタム習慣: is_customフラグをtrueに設定して、ユーザーが作成した習慣であることを明示
- ページリフレッシュ: `router.refresh()`でServer Componentを再実行して最新データを表示

#### 成果物

**コード:**

- `app/dashboard/habit-list.tsx`: 習慣追加機能を全面実装
  - `handleOpenModal`: モーダルを開く関数
  - `handleCloseModal`: モーダルを閉じる関数
  - `handleSaveHabit`: 習慣の作成・保存処理
  - モーダルUI: 習慣名、種類、入力タイプ、報酬設定、オプション設定の入力フォーム

#### 学んだこと

- モーダル実装パターン: ToDo追加機能と同様のパターンで実装
- フォーム状態管理: `useState`でフォームデータを管理
- バリデーション: 習慣名の必須チェックを実装
- display_order自動計算: 最大値を取得して+1する方法

#### 次回予定

**Phase 3 残りタスク:**

- 習慣管理画面の実装（Phase 4以降でも可）
- AI判定機能の実装（体調スコア・気分スコア判定）
- AI生成機能の実装（アドバイス・あらすじ生成）

### 251209-火（続き2）

#### 実施内容

**Phase 3（機能拡張）継続 - AI判定機能の基盤実装:**

- AI判定機能の基盤実装
  - OpenAI SDKインストール
    - `npm install openai`でOpenAI SDKをインストール
  - OpenAI API統合（GPT-4o mini）
    - `lib/ai/openai.ts`: OpenAIクライアント初期化関数とプロンプト生成関数を作成
    - `getOpenAIClient`: 環境変数からAPIキーを取得してOpenAIクライアントを初期化
    - `createJudgmentPrompt`: AI判定用プロンプト生成関数
    - `createAdvicePrompt`: AIアドバイス生成用プロンプト生成関数
    - `createStoryPrompt`: AIあらすじ生成用プロンプト生成関数
  - Next.js API Route作成
    - `app/api/ai/judgment/route.ts`: 体調スコア・気分スコア判定API
    - `app/api/ai/advice/route.ts`: 厳しめコーチングアドバイス生成API
    - `app/api/ai/story/route.ts`: RPG物語風あらすじ生成API
  - APIキーの環境変数設定
    - `.env.local`に`OPENAI_API_KEY`を設定する必要があることをREADMEに記載

#### 決定事項

- モデル選択: GPT-4o miniを使用（コスト効率が良い）
- API設計: Next.js API Routeを使用（サーバー側でAPIキーを保護）
- プロンプト設計: システムメッセージとユーザーメッセージを分離
- エラーハンドリング: 各API Routeで適切なエラーレスポンスを返す
- レスポンス形式: JSON形式で統一

#### 成果物

**コード:**

- `lib/ai/openai.ts`: OpenAI API統合ユーティリティ
  - `getOpenAIClient`: OpenAIクライアント初期化関数
  - `createJudgmentPrompt`: AI判定用プロンプト生成関数
  - `createAdvicePrompt`: AIアドバイス生成用プロンプト生成関数
  - `createStoryPrompt`: AIあらすじ生成用プロンプト生成関数

- `app/api/ai/judgment/route.ts`: AI判定API Route
  - POST: 日誌内容から体調スコア・気分スコアを判定

- `app/api/ai/advice/route.ts`: AIアドバイス生成API Route
  - POST: 厳しめコーチングアドバイスを生成

- `app/api/ai/story/route.ts`: AIあらすじ生成API Route
  - POST: RPG物語風あらすじを生成

**パッケージ:**

- `openai`: OpenAI SDK（最新版）

**ドキュメント:**

- `README.md`: 環境変数設定方法を追記

#### 学んだこと

- OpenAI SDK: `openai`パッケージの基本的な使い方
- Next.js API Route: サーバー側でAPIキーを保護する方法
- プロンプト設計: システムメッセージとユーザーメッセージの使い分け
- JSON形式レスポンス: `response_format: { type: 'json_object' }`の使い方
- エラーハンドリング: API Routeでの適切なエラーレスポンスの返し方

#### 次回予定

**Phase 3 完了！次はPhase 4へ:**

- 習慣管理画面の実装（Phase 4以降でも可）
- UI/UX改善（shadcn/ui導入、レスポンシブデザイン調整）

### 251209-火（続き3）

#### 実施内容

**Phase 3（機能拡張）完了 - AI判定機能・AI生成機能の実装:**

- AI判定機能の実装
  - journal-form.tsxにAI判定機能を統合
    - `/api/ai/judgment` APIを呼び出して体調スコア・気分スコアを判定
    - 判定結果をUIに表示（体調スコア・気分スコア・判定理由）
    - ローディング状態の管理（`isJudging`ステート）
  - ポイント/EXP自動計算ロジック実装
    - 体調スコアと気分スコアの平均からポイントを計算（0-50ポイント）
    - 体調スコア → 身体EXP、気分スコア → 精神EXP、平均 → 頭脳EXP
  - daily_logsへのAI判定結果保存機能実装
    - `ai_condition_body`, `ai_condition_mood`, `ai_points_earned`, `ai_exp_body`, `ai_exp_mind`, `ai_exp_spirit`を保存
    - profilesテーブルのポイント/EXPも自動更新
- AI生成機能の実装
  - AIアドバイス生成機能実装
    - `/api/ai/advice` APIを呼び出して厳しめコーチングアドバイスを生成
    - 判定結果がある場合のみ生成可能
    - 生成結果を`ai_advice`フィールドに保存
    - ローディング状態の管理（`isGeneratingAdvice`ステート）
  - AIあらすじ生成機能実装
    - `/api/ai/story` APIを呼び出してRPG物語風あらすじを生成
    - 生成結果を`ai_story_past`フィールドに保存
    - ローディング状態の管理（`isGeneratingStory`ステート）
  - AI生成結果の表示機能実装
    - 判定結果、アドバイス、あらすじをUIに表示
    - 既存データがある場合は初期値として表示
    - 生成ボタンは未生成の場合のみ表示

#### 決定事項

- ポイント計算: 体調スコアと気分スコアの平均から0-50ポイントを計算
- EXP計算: 体調スコア → 身体EXP、気分スコア → 精神EXP、平均 → 頭脳EXP
- アドバイス生成: AI判定を実行した後でないと生成できない（判定結果が必要）
- あらすじ生成: 判定結果がなくても生成可能（独立した機能）
- データ保存: 各AI生成結果をdaily_logsテーブルに保存
- プロファイル更新: AI判定時にprofilesテーブルのポイント/EXPも自動更新

#### 成果物

**コード:**

- `app/dashboard/journal-form.tsx`: AI判定・生成機能を全面実装
  - `handleAIJudgment`: AI判定実行関数
  - `calculatePointsAndExp`: ポイント/EXP自動計算関数
  - `handleGenerateAdvice`: AIアドバイス生成関数
  - `handleGenerateStory`: AIあらすじ生成関数
  - AI判定結果表示UI: 体調スコア・気分スコア・判定理由・獲得ポイント/EXPを表示
  - AIアドバイス表示UI: 厳しめコーチングアドバイスを表示
  - AIあらすじ表示UI: RPG物語風あらすじを表示

#### 学んだこと

- API呼び出し: `fetch` APIを使用してNext.js API Routeを呼び出す方法
- エラーハンドリング: API呼び出し時のエラーハンドリングとユーザーへの通知
- ローディング状態管理: 非同期処理中のローディング状態を管理する方法
- データ保存: Supabaseへの保存とprofilesテーブルの更新を同時に実行
- UI設計: 判定結果に基づいて条件付きでボタンを表示する方法

#### 次回予定

**Phase 3 完了！次はPhase 4へ:**

- 習慣管理画面の実装（Phase 4以降でも可）
- UI/UX改善（shadcn/ui導入、レスポンシブデザイン調整）

### 251209-火（続き）

#### 実施内容

**Phase 3（機能拡張）継続 - 完了済みタスクの報酬計算・反映機能実装:**

- 完了済みタスクの報酬計算・反映機能実装
  - 報酬計算ロジック実装
    - `calculateReward`関数: is_specialがtrueの場合のみ報酬を計算（sp_points, sp_exp_body, sp_exp_mind, sp_exp_spirit）
    - is_specialがfalseの場合は報酬なし（0）
  - todo_logsへの記録機能実装
    - `handleTaskCompletion`: タスク完了時にtodo_logsに記録を作成/更新（UPSERT）
    - `handleTaskUncompletion`: タスク未完了時にtodo_logsから記録を削除
    - UNIQUE制約（daily_log_id, todo_id）を考慮したUPSERT処理
  - profilesテーブルのポイント/EXP更新機能実装
    - タスク完了時: profilesテーブルのポイント/EXPに報酬を加算
    - タスク未完了時: profilesテーブルのポイント/EXPから報酬を減算（0未満にならないように制限）
  - 実装場所
    - `kanban-board.tsx`: ドラッグ&ドロップで完了済みに移動したとき
    - `todo-summary-tab.tsx`: モーダルでステータスを'completed'に変更したとき

#### 決定事項

- 報酬計算: is_specialがtrueの場合のみ報酬を付与（通常タスクは報酬なし）
- UPSERT処理: todo_logsのUNIQUE制約を考慮して、既存レコードは更新、新規は作成
- 報酬削除: タスクを未完了に戻した場合、報酬を減算してtodo_logsから記録を削除
- ページリフレッシュ: 報酬反映後はページをリロードして最新データを表示（kanban-board.tsxではwindow.location.reload()を使用）

#### 成果物

**コード:**

- `app/dashboard/kanban-board.tsx`: 報酬計算・反映機能を実装
  - `calculateReward`: 報酬計算関数
  - `handleTaskCompletion`: タスク完了時の報酬付与処理
  - `handleTaskUncompletion`: タスク未完了時の報酬削除処理
  - `handleDragEnd`: ドラッグ終了時に報酬計算・反映を実行

- `app/dashboard/todo-summary-tab.tsx`: 報酬計算・反映機能を実装
  - `calculateReward`: 報酬計算関数
  - `handleTaskCompletion`: タスク完了時の報酬付与処理
  - `handleTaskUncompletion`: タスク未完了時の報酬削除処理
  - `handleSaveTodo`: ToDo保存時にステータス変更を検知して報酬計算・反映を実行

- `app/dashboard/dashboard-tabs.tsx`: TodoSummaryTabにdailyLogIdをpropsで渡す

#### 学んだこと

- UPSERT処理: Supabaseの`.upsert()`メソッドと`onConflict`オプションの使い方
- 報酬計算ロジック: is_specialフラグに基づいた条件分岐
- データ整合性: タスク完了/未完了時の報酬の付与/削除を確実に実行
- エラーハンドリング: 各処理でエラーが発生しても他の処理に影響しないように実装

#### 次回予定

**Phase 3 残りタスク:**

- 習慣の追加機能実装（モーダル方式）
- AI判定機能の実装準備

### 251128-金（深夜）

#### 実施内容

**Phase 3（機能拡張）継続 - ToDoサブタスク機能の実装:**

- ToDoサブタスク機能の実装
  - データ取得機能実装
    - `dashboard/page.tsx`でtodo_subtasksテーブルからデータを取得
    - `DashboardTabsProps`に`todoSubtasks`を追加
    - `dashboard-tabs.tsx`でpropsとして`TodoSummaryTab`に渡す
  - サブタスク表示機能実装
    - 展開/折りたたみ機能（`expandedTodos`ステートで管理）
    - チェックボックスで完了状態を表示
    - アクティブタスクと完了済みタスクの両方にサブタスク表示
  - サブタスクCRUD機能実装
    - 追加機能: 「+ 追加」ボタンでサブタスクを追加（インライン編集）
    - 編集機能: 「編集」ボタンでサブタスク名を編集（インライン編集）
    - 削除機能: 「削除」ボタンでサブタスクを削除（確認ダイアログ）
    - 完了状態切り替え: チェックボックスで完了/未完了を切り替え
  - データ更新後の自動リフレッシュ機能実装
    - `router.refresh()`でServer Componentを再実行

#### 決定事項

- インライン編集方式: サブタスクの追加・編集はインラインで行う（モーダル不要）
- 展開/折りたたみ: 各ToDoタスクごとに独立して展開/折りたたみ可能
- 完了済みタスクのサブタスク: 表示のみ（編集・削除不可、チェックボックスは無効化）
- キーボードショートカット: Enterで追加、Escapeでキャンセル

#### 成果物

**コード:**

- `app/dashboard/page.tsx`: todo_subtasksデータ取得機能追加
- `app/dashboard/dashboard-tabs.tsx`: todoSubtasksをpropsで受け取り、TodoSummaryTabに渡す
- `app/dashboard/todo-summary-tab.tsx`: サブタスク表示・CRUD機能を全面実装
  - `getSubtasksForTodo`: サブタスク取得関数
  - `toggleSubtaskExpansion`: 展開/折りたたみ関数
  - `handleToggleSubtaskCompletion`: 完了状態切り替え関数
  - `handleAddSubtask`: サブタスク追加関数
  - `handleEditSubtask`: サブタスク編集関数
  - `handleDeleteSubtask`: サブタスク削除関数
  - サブタスクUI: 展開/折りたたみボタン、チェックボックス、インライン編集フォーム

**型定義:**

- `lib/types.ts`: `DashboardTabsProps`に`todoSubtasks: TodoSubtask[]`を追加

#### 学んだこと

- インライン編集パターン: モーダルを使わずに直接編集できるUIパターン
- 展開/折りたたみ実装: `Set`を使って複数の展開状態を管理
- 条件付きレンダリング: `(() => { ... })()`で即時関数を使って複雑な条件分岐を実装
- キーボードショートカット: `onKeyDown`でEnter/Escapeキーを処理
- データ取得の最適化: todosが空の場合はサブタスク取得をスキップ

#### 次回予定

**Phase 3 残りタスク:**

- 完了済みタスクの報酬計算・反映機能（todo_logsへの記録）
- 習慣の追加機能実装（モーダル方式）
- AI判定機能の実装準備

### 251128-金（夜）

#### 実施内容

**Phase 3（機能拡張）開始 - ToDoサマリータブのCRUD機能実装:**

- ToDoサマリータブのCRUD機能実装
  - ToDo追加機能実装
    - 「+ 新規タスク」ボタンに機能追加
    - モーダルフォームの作成（タスク名、SPタスクフラグ、報酬設定、ステータス、期限）
    - SupabaseへのINSERT処理実装
    - ユーザーID取得とdisplay_order自動計算
  - ToDo編集機能実装
    - 「📝編集」ボタンに機能追加
    - 既存ToDoデータをモーダルに読み込み
    - SupabaseへのUPDATE処理実装
  - ToDo削除機能実装
    - 「🗑削除」ボタンに機能追加
    - 確認ダイアログ表示（誤削除防止）
    - SupabaseへのDELETE処理実装
  - データ更新後のページリフレッシュ機能実装
    - `router.refresh()`を使用してServer Componentを再実行
    - データベース更新後に自動的に最新データを表示

#### 決定事項

- モーダル方式: 追加・編集を同じモーダルで実装（`editingTodo`で切り替え）
- フォームバリデーション: タスク名の必須チェックのみ実装（簡易版）
- 削除確認: `confirm()`ダイアログを使用（将来的にカスタムモーダルに変更可能）
- ページリフレッシュ: `router.refresh()`でServer Componentを再実行して最新データを取得

#### 成果物

**コード:**

- `app/dashboard/todo-summary-tab.tsx`: CRUD機能を全面実装
  - `handleOpenCreateModal`: 新規作成モーダルを開く
  - `handleOpenEditModal`: 編集モーダルを開く
  - `handleSaveTodo`: ToDoの作成・更新処理
  - `handleDeleteTodo`: ToDoの削除処理
  - モーダルUI: タスク名、SPタスク設定、報酬設定、ステータス、期限の入力フォーム

#### 学んだこと

- `router.refresh()`: Next.js App RouterでServer Componentを再実行する方法
- Supabase INSERT/UPDATE/DELETE: `.insert()`, `.update()`, `.delete()`メソッドの使い方
- モーダル実装パターン: 同じモーダルで追加・編集を切り替える方法
- フォーム状態管理: `useState`でフォームデータを管理し、編集時は既存データで初期化
- 確認ダイアログ: `confirm()`で削除前に確認を取る方法

#### 次回予定

**Phase 3 残りタスク:**

- ToDoサブタスク（子チェックリスト）機能の実装
- 完了済みタスクの報酬計算・反映機能（todo_logsへの記録）
- 習慣の追加機能実装（モーダル方式）
- AI判定機能の実装準備

### 251128-金（午後）

#### 実施内容

**Phase 2（データベース統合）完了 - ドラッグ&ドロップ機能実装:**

- ドラッグ&ドロップ機能実装
  - `@dnd-kit/core`、`@dnd-kit/sortable`、`@dnd-kit/utilities`をインストール
  - `kanban-board.tsx`にDndContextを追加
  - 各カラムをDroppableに実装（アクティブ/進行中/完了済み）
  - 各タスクカードをDraggableに実装（完了済みは除く）
  - ドラッグ終了時のステータス変更処理を実装
  - ステータス変更時のデータベース更新処理を実装（Supabase）
  - 完了済みに移動した場合、`completed_at`を自動設定
  - ドラッグ中の視覚的フィードバックを追加（DragOverlay、ドロップ領域のハイライト）
  - 楽観的更新: ドラッグ終了時に即座にUIを更新、その後データベースを更新
  - エラーハンドリング: 更新失敗時は元の状態に戻す

- テストデータ挿入機能の改善
  - `profiles`テーブルの存在確認と自動作成機能を追加
  - 外部キー制約違反を解消

#### 決定事項

- ドラッグ&ドロップ: `@dnd-kit/core`を使用（React 19完全互換、アクセシビリティ対応）
- 誤操作防止: 8px以上移動しないとドラッグ開始しない（`activationConstraint`）
- 完了済みカード: ドラッグ不可（固定表示）
- 楽観的更新: UIを即座に更新し、その後データベースを更新（UX向上）
- エラーハンドリング: 更新失敗時は元の状態に戻し、ユーザーに通知

#### 成果物

**コード:**

- `app/dashboard/kanban-board.tsx`: ドラッグ&ドロップ機能を全面実装
  - `DraggableTodoCard`: ドラッグ可能なタスクカードコンポーネント
  - `DroppableColumn`: ドロップ可能なカラムコンポーネント
  - `CompletedTodoCard`: 完了済みカードコンポーネント（ドラッグ不可）
  - `handleDragEnd`: ドラッグ終了時のステータス変更とDB更新処理

- `app/test-todos/page.tsx`: `profiles`自動作成機能を追加

**パッケージ:**

- `@dnd-kit/core`: ドラッグ&ドロップのコア機能
- `@dnd-kit/sortable`: ソート機能（将来の拡張用）
- `@dnd-kit/utilities`: ユーティリティ関数

#### 学んだこと

- `@dnd-kit/core`の基本概念: `DndContext`、`useDraggable`、`useDroppable`、`DragOverlay`
- 楽観的更新パターン: UIを即座に更新し、その後データベースを更新することでUXを向上
- `PointerSensor`と`activationConstraint`: 誤操作を防ぐための設定
- `CSS.Translate.toString()`: ドラッグ中の位置を計算
- ステータス変更時の`completed_at`管理: 完了済みに移動した場合のみ設定、それ以外はnull

#### 次回予定

**Phase 3 準備:**

- ToDoサマリータブの実装
- 完了済みタスクの報酬計算・反映機能（todo_logsへの記録）
- 習慣管理画面の実装
- AI判定機能の実装準備


### 251128-金

#### 実施内容

**Phase 2（データベース統合）継続 - ToDoカンバンDB連携:**

- ToDoカンバンのデータベース連携実装
  - `app/dashboard/page.tsx`でtodosとtodo_logsを取得
    - ステータス別にtodosを分類（active/in_progress/completed）
    - 今日のdaily_logsと紐づくtodo_logsを取得
    - データをpropsで`dashboard-tabs.tsx`に渡す
  - `kanban-board.tsx`をデータベース連携対応に修正
    - モックデータを削除し、propsからデータを受け取る
    - todosとtodo_logsをマージして表示
    - 期限超過判定ロジック実装（due_dateと今日の日付を比較）
    - ステータス別にカラム表示（アクティブ/進行中/完了済み）
    - アイコン表示ロジック（SPタスクは⚠️、通常タスクは📄など）
  - ステータス変更機能の準備
    - カードクリック時のステータス変更処理（Phase 2後半で実装予定）
    - ドラッグ&ドロップ対応準備（Phase 2後半で実装予定）

#### 決定事項

- 期限超過判定: `due_date`が今日より前かつ`status !== 'completed'`の場合に期限超過と判定
- アイコン表示: `is_special`がtrueの場合は⚠️、それ以外は📄をデフォルト表示（将来的にカスタマイズ可能に）
- ステータス管理: `todos.status`で3つのカラムに分類（'active'/'in_progress'/'completed'）
- 完了済みタスクの表示: `completed_at`が存在する場合は完了日を表示

#### 成果物

**コード:**

- `app/dashboard/page.tsx`: todos/todo_logs取得機能追加、ステータス別分類処理追加
- `app/dashboard/dashboard-tabs.tsx`: todos/todo_logsをpropsで受け取り、KanbanBoardに渡す
- `app/dashboard/kanban-board.tsx`: データベース連携対応に全面リファクタリング、期限超過判定実装

#### 学んだこと

- ステータス別データ分類: `todos`を`status`でフィルタリングして3つのカラムに分割
- 日付比較ロジック: JavaScriptの`Date`オブジェクトと`due_date`（DATE型）の比較方法
- 条件付きスタイリング: 期限超過タスクの赤枠表示（`isOverdue`フラグを使用）
- データマッピング: DBカラム名（`task_name`, `sp_points`）をUI用名（`title`, `specialPoints`）に変換
- 完了済みタスクの扱い: `completed_at`が存在する場合の表示ロジック

#### 次回予定

**Phase 2 残りタスク:**

- ドラッグ&ドロップ機能実装（@dnd-kit/core）
  - ライブラリインストール
  - カンバンボードにD&D機能追加
  - ステータス変更時のデータベース更新処理
- ToDoカンバンのステータス変更機能（クリック/ドラッグでステータス更新）
- 完了済みタスクの報酬計算・反映機能（todo_logsへの記録）


### 251121-金

#### 実施内容

**コードリーディング継続:**
- `app/signup/page.tsx`をリーディング
  - ログインページとの違いを確認（パスワード確認フィールド、バリデーション処理）
  - `signUp`メソッドと`signInWithPassword`の違いを理解
  - OAuthサインアップの実装を確認

- `app/dashboard`配下の全ファイルをリーディング
  - `page.tsx`: Server Componentでの認証チェック、データ取得、props渡し
  - `logout-button.tsx`: ログアウト処理
  - `dashboard-tabs.tsx`: タブ切り替え、子コンポーネントへのprops渡し
  - `habit-list.tsx`: 習慣リスト表示、データベース更新（UPSERT処理）
  - `journal-form.tsx`: 日誌フォーム、権利管理、データベース更新
  - `kanban-board.tsx`: カンバンボード表示（モックデータ、Phase 2でDB連携予定）

#### 決定事項

（記載予定）

#### 成果物

（記載予定）

#### 学んだこと

**サインアップ画面:**
- クライアント側バリデーション: パスワード一致チェック、長さチェックをフォーム送信前に実行
- `signUp`と`signInWithPassword`の違い: 新規登録 vs 既存ユーザーのログイン
- `emailRedirectTo`オプション: メール確認リンクのリダイレクト先を指定
- OAuthの「サインアップ」と「ログイン」は同じ`signInWithOAuth`メソッドを使用（初回アクセス時に自動アカウント作成）

**ダッシュボード:**
- Server Component vs Client Componentの使い分け: データ取得はServer、インタラクションはClient
- `.single()`メソッド: 1件のみ取得（配列ではなくオブジェクトを返す）
- UPSERT処理: 既存レコードがあれば更新、なければ作成
- データマージパターン: `habits`と`habit_logs`をマージして統合表示
- `useEffect`でprops変更を監視: propsが変更されたら状態を更新
- 条件付きクエリ: `dailyLogId`が存在する場合のみ取得
- データマッピング: DBカラム名（`exp_body`）をUI用名（`body`）に変換

#### 次回予定

**コードリーディング継続:**
- `app/dashboard/page.tsx`
- `app/dashboard/dashboard-tabs.tsx`
- `app/dashboard/habit-list.tsx`
- `app/dashboard/journal-form.tsx`
- `app/dashboard/kanban-board.tsx`
- `app/dashboard/logout-button.tsx`

### 251120-木

- コードリーディング
web-app/gol-web/app/page.tsx
web-app/gol-web/app/layout.tsx
web-app/gol-web/app/login/page.tsx

次回、以下画面のリーディング予定
web-app/gol-web/app/signup/page.tsx
web-app/gol-web/app/dashboardの中

#### 実施内容

- Supabase認証フロー（`handleLogin`/`signInWithPassword`/OAuth）を再確認し、Promiseと`async/await`の挙動や`{ data, error }`の扱いを整理
- Reactフォーム制御（`htmlFor`/`id`紐付け、`value={state}`、`React.FormEvent`型）やOAuth`provider`引数のユニオン型など、コード理解を深めた内容をドキュメント化
- Tailwind CSSユーティリティ学習方針をまとめたメモを作成し、活用パターンと参照先を整理


#### 決定事項

- `Memo/`配下を編集する際は必ず`A-Dta2/Memo/0-rule.md`の書式ルール（見出し・改行・リスト）に従う運用を継続
- Tailwindユーティリティは基本パターンのみ記憶し、詳細は補完・ドキュメントで随時参照する方針


#### 成果物

- `A-Dta2/Memo/tailwind-css-memo.md`: ユーティリティクラスの覚え方、参照リンク、代表的パターンを追記
- `docs/4-dev-log.md`: 251120-木の学習ログを詳細に追記（認証フロー、フォーム制御、BaaS理解など）


#### 学んだこと

- Supabase Authが提供するOAuthプロバイダー識別子と、`provider: 'google' | 'apple'`の型安全性
- `window.location.origin`を使った環境依存しないリダイレクトURL生成方法
- `next/font/google`でGeistフォントを`--font-*`変数として適用する手順


#### 次回予定

- Phase 2未完了タスク（ToDoカンバンDB連携、D&D機能）に着手できるよう、今回整理した知識を実装へ反映
- Tailwindメモに追記したパターンを実コードで試し、運用感をフィードバック


### 251118-火

#### 実施内容

（記載予定）

#### 決定事項

（記載予定）

#### 成果物

（記載予定）

#### 学んだこと

（記載予定）

#### 次回予定

（記載予定）


### 251114-木

#### 実施内容

**Phase 2（データベース統合）継続:**

- ダッシュボードでprofilesデータ表示（DB連携）実装
  - Supabaseからprofilesテーブルを取得
  - ハードコーディングされたデータをデータベース連携に置き換え
  - エラーハンドリング実装（profilesが存在しない場合のデフォルト値処理）
  - データマッピング（exp_body → body, exp_mind → intellect, exp_spirit → mind）

- 習慣リストのデータベース連携実装
  - `page.tsx`でhabitsとhabit_logsを取得
  - 今日の`daily_logs`を取得（なければ自動作成）
  - `dashboard-tabs.tsx`でデータをpropsで受け渡し
  - `habit-list.tsx`をデータベース連携対応に修正
  - habitsとhabit_logsをマージして表示
  - チェックボックス・数値入力の変更時にhabit_logsを更新/作成
  - 良習慣/悪習慣/ボーナスの分類表示
  - input_typeに応じた数値入力フィールドの表示制御

- 日誌フォームのデータベース連携実装
  - `page.tsx`でdaily_logsの全データを取得（`select('*')`）
  - `dashboard-tabs.tsx`でdailyLogデータをpropsで受け渡し
  - `journal-form.tsx`をデータベース連携対応に修正
  - daily_logsから取得したデータを初期値として表示
  - 日誌本文（journal_text）、一言感想（one_line_comment）の保存機能実装
  - 権利の利用回数（right_a_count ~ right_x_count）の保存機能実装
  - 保存時に`daily_logs`テーブルを更新（UPDATE）

#### 決定事項

- フォールバック処理: profilesが存在しない場合（トリガー未実行など）はデフォルト値を使用

- daily_logsの自動作成: 習慣リスト表示時に今日のdaily_logsが存在しない場合は自動作成

- habit_logsの更新方式: 既存ログは更新、新規は作成（UPSERT的な動作）

#### 成果物

**コード:**

- `app/dashboard/page.tsx`: profilesデータ取得・表示機能追加、habits/habit_logs取得機能追加、daily_logs全データ取得機能追加

- `app/dashboard/dashboard-tabs.tsx`: propsでhabits/habit_logs/dailyLogを受け取り、HabitList/JournalFormに渡す

- `app/dashboard/habit-list.tsx`: データベース連携対応に全面リファクタリング

- `app/dashboard/journal-form.tsx`: データベース連携対応に全面リファクタリング、保存機能実装

#### 学んだこと

- Supabaseの`.from().select().eq().single()`による単一レコード取得
- Server Componentでの非同期データ取得
- エラーハンドリングとフォールバック処理の実装
- habitsとhabit_logsのマージ処理
- Client ComponentからSupabaseクライアントでデータ更新
- daily_logsとhabit_logsのリレーション管理
- daily_logsテーブルのUPDATE処理
- 権利の利用回数をデータベースに保存する方法

#### 次回予定

**Phase 2 残りタスク:**

- ToDoカンバンのDB連携
- ドラッグ&ドロップ機能実装


### 251113-水

#### 実施内容

**Phase 2（データベース統合）開始:**

- データベース設計書作成
  - 6テーブル設計（profiles, daily_logs, habits, habit_logs, todos, todo_logs）
  - RLS設定定義
  - 初期データ挿入関数定義

- Supabaseテーブル作成実行
  - supabase-setup.sql 作成
  - 6つのテーブル作成成功
  - RLS有効化とポリシー設定（30以上のポリシー）

- 新規ユーザー登録時の自動処理実装
  - トリガー関数作成（handle_new_user）
  - profilesテーブル自動作成
  - デフォルト習慣33項目自動挿入

#### 決定事項

- データベース正規化: マスタテーブル（habits, todos）とログテーブル（habit_logs, todo_logs）を分離

- RLS方式: データベース層でセキュリティ制御（アプリ側でのチェック不要）

#### 成果物

**ドキュメント:**

- `docs/06-database-schema.md`: データベース設計書
- `docs/supabase-setup.sql`: テーブル作成SQL
- `docs/supabase-trigger-setup.sql`: トリガー関数SQL

**Supabase:**

- 6つのテーブル作成完了
- RLSポリシー30以上設定完了
- トリガー関数設定完了

#### 学んだこと

- インデックス: 検索高速化の仕組み
- RLS: Row Level Securityでユーザーデータ保護
- CRUD: Create/Read/Update/Deleteの基本操作
- トリガー関数: イベント発火時の自動処理

#### 次回予定

**Phase 2 残りタスク:**

- ダッシュボードでprofilesデータ表示（DB連携）
- 習慣リストのDB連携
- 日誌フォームのDB連携
- ToDoカンバンのDB連携
- ドラッグ&ドロップ機能実装




### 251112-水

#### 実施内容

**Phase 1 UI実装の完了:**

- 習慣リストUI実装
  - 良習慣（10項目）: すべてに数値入力フィールド
  - 悪習慣（6項目）: チェックボックス形式
  - ボーナス: Completeボーナス
  - 回数/距離カウンター対応（整数/小数点）
  - ポイント自動計算・表示
  - 左側の縦軸揃え（レイアウト統一）
  - [+ 習慣を追加] [⚙️ 習慣を管理] ボタン（Phase 2実装予定）

- 日誌フォームUI実装
  - 今日の日誌: テキストエリア（3000文字、文字数カウント）
  - 一言感想: テキストエリア（500文字、文字数カウント）
  - 本日の利用ポイント: 数値入力（権利A～X、7項目）
  - ポイント消費計算・合計表示（赤色表示）
  - 日誌を保存ボタン（モック動作）
  - AI判定エリア（プレースホルダー、Phase 2実装予定）

#### 決定事項

- 習慣の数値入力: すべての良習慣に数値入力フィールドを配置（後で棲み分け可能）

- 単位表示: 回数や距離の単位（「回」「km」）は非表示に

- 利用ポイント: チェックボックスではなく数値入力（スピン）で実装

- 習慣管理画面: Phase 2以降で実装（MVP優先）

#### 成果物

**コード:**

- `app/dashboard/habit-list.tsx`: 習慣リストコンポーネント（新規作成）

- `app/dashboard/journal-form.tsx`: 日誌フォームコンポーネント（新規作成）

#### 学んだこと

- 数値入力フィールド（type="number"）の実装（min/max/step属性）

- 配列操作によるステート管理（map関数での部分更新）

- レイアウト統一のためのスペーサー配置（縦軸揃え）

- 文字数カウント機能（textarea.length）

- ポイント計算のロジック（reduce関数）

#### 次回予定

**Phase 2（データベース統合）:**

- Supabaseテーブル設計
  - profiles（ユーザープロファイル）
  - daily_logs（日誌）
  - habits（習慣マスタ）
  - todos（ToDoマスタ）
  - habit_logs（習慣記録）
  - todo_logs（ToDo記録）

- Row Level Security（RLS）設定

- API連携実装

- CRUD操作実装

**機能追加:**

- ドラッグ&ドロップ機能（@dnd-kit/core）

- ToDoサマリータブの実装

- 習慣管理画面

### 251108-土

#### 実施内容

**Phase 1 UI実装の継続:**

- ダッシュボードヘッダー（ステータスバー）実装
  - 固定ヘッダー（sticky）
  - ユーザー名、レベル、クラス、ポイント表示
  - EXP表示（身体/頭脳/精神）
  - 設定ボタン、ログアウトボタン
  - ハードコーディングデータ使用

- タブナビゲーション実装
  - 日誌タブ / ToDoサマリータブ
  - アクティブタブの視覚的強調（シアン色+下線）
  - タブ切り替え機能（useState使用）
  - ホバー時のポインター表示

- カンバンボードモック実装
  - 3カラムレイアウト（アクティブ/進行中/完了済み）
  - カード型タスク表示（アイコン、タスク名、SP、EXP、期限）
  - 期限超過タスクは赤枠表示
  - 完了済みタスクは取り消し線+透明度
  - ホバー時シアン枠線
  - 仮データで動作確認完了

#### 決定事項

- レスポンシブ対応: Phase 4で実装（まずPC版で機能完成を優先）

- ドラッグ&ドロップ: Phase 2で実装（@dnd-kit/core導入予定）

- ハードコーディング方式: Phase 1ではベタ書きデータでUI確認、Phase 2でデータベース化

#### 成果物

**コード:**

- `app/dashboard/dashboard-tabs.tsx`: タブナビゲーションコンポーネント

- `app/dashboard/kanban-board.tsx`: カンバンボードコンポーネント

- `app/dashboard/page.tsx`: ダッシュボードページ（ヘッダー追加）

- `app/dashboard/logout-button.tsx`: ログアウトボタン（スタイル調整）

**ドキュメント:**

- `3-project-progress.md`: Phase 1チェックリスト追加、本日分更新

- `4-dev-log.md`: 詳細ログ更新（251108-土セクション作成）

#### 学んだこと

- ベタ書き（ハードコーディング）の使い分け（プロトタイプ段階でのメリット）

- Tailwindのstickyポジショニング（固定ヘッダー実装）

- タブUIの実装パターン（useState + 条件分岐レンダリング）

- grid-cols-3 での3カラムレイアウト

- 条件付きスタイリング（期限超過、完了状態の視覚化）

#### 次回予定

**Phase 1 残りタスク:**

- 習慣リストUI実装

- 日誌フォームUI実装


### 251107-金

#### 実施内容

**設計・準備フェーズ:**

- GOL Web版の開発方針決定

- MD版の機能理解・整理

- 設計書・ワイヤーフレーム作成
  - 参考APP（Habitica/Trello）の特徴整理
  - 技術要件確定（Next.js 16、React 19、Tailwind v4、Supabase）
  - ライブラリ選定（@dnd-kit/core、shadcn/ui）
  - ドキュメント書式統一ルール策定（改行ルール、日付降順ルール）

- ドキュメント構成確立
  - `_INDEX.md`: ドキュメント目次
  - `z-project-draft.md`: AIへの指示・草案（AI編集禁止）
  - `1-spec-sheet.md`: 決定稿の実装ルール
  - `appendix/wireframe.md`: ワイヤーフレーム（6画面分）
  - `3-project-progress.md`: 進捗表（本ファイル）
  - `4-dev-log.md`: 開発ログ（雑多でOK）
  - `5-dta2-memo.md`: AI時代のエンジニア観

- 開発スタイル決定
  - Cursor AI（Chat）とのペアプログラミング方式
  - 学習重視、ブラックボックス化を避ける

- デザイン・実装フロー確定
  - A: イメージ言語化 ✅
  - B: ワイヤーフレーム作成 ✅
  - C: プロトタイプ作成 🔄 開始
  - D: 細部作り込み

**プロジェクト作成・環境構築:**

- Next.js 16 プロジェクト作成
  - プロジェクト名: `gol-web`
  - ディレクトリ構成: `web-app/gol-web/`（サブディレクトリ型）
  - TypeScript、ESLint、Tailwind v4、App Router

- Supabase環境構築
  - プロジェクト作成: `gol-web`（Tokyo リージョン）
  - 環境変数設定（`.env.local`）
  - Supabaseクライアント初期化（`lib/supabase/client.ts`、`lib/supabase/server.ts`）
  - 接続テスト成功

- セキュリティ設定
  - `.gitignore` 作成（`z-project-draft.md` を除外、API KEY保護）

**認証システム実装（Phase 0 完了）:**

- ログイン画面実装（`/login`）
  - Email/Password認証
  - Google OAuth対応
  - エラーハンドリング
  - ローディング状態表示

- サインアップ画面実装（`/signup`）
  - Email/Password登録
  - パスワード確認・バリデーション
  - Google OAuth対応
  - エラーハンドリング

- ダッシュボード実装（`/dashboard`）
  - 認証状態チェック（未ログイン時リダイレクト）
  - ユーザー情報表示
  - ログアウト機能

- 全機能テスト完了
  - ログイン/サインアップ/ログアウトのフロー確認
  - エラーハンドリング動作確認

#### 決定事項

- プロジェクトディレクトリ名: `gol-web`

- ドキュメント構成: 7ファイル体制（_INDEX + 00〜05）

- 改行ルール: docs内全ファイルで統一

- 認証プロバイダー: Email/Password、Google（Appleは中止）

- 開発方針: UI先行（モックデータ）→ データベース接続（Phase 2）

- Phase区分: Phase 0（認証・基盤）完了 → Phase 1（UI実装）へ

- 技術導入タイミング:
  - shadcn/ui: Phase 2-3で導入（現在は手書きTailwind）
  - コンポーネント集: Phase 3-4で作成（/components-showcaseページ方式）
  - Atomic Design: Phase 2-3で段階的に導入
  - テスト: Phase 4-5で導入（E2Eテスト → 単体テスト）
  - Mermaidフローチャート: Phase 2で追加予定

#### 成果物

**ドキュメント:**

- `_INDEX.md`: ドキュメント目次

- `z-project-draft.md`: AI指示・草案

- `1-spec-sheet.md`: 設計書（更新版、現在の進捗状況を反映）

- `appendix/wireframe.md`: ワイヤーフレーム

- `3-project-progress.md`: 進捗表（本ファイル）

- `4-dev-log.md`: 開発ログ（詳細記録）

- `5-dta2-memo.md`: エンジニア観・学習メモ

**コード:**

- `app/page.tsx`: トップページ（`/login`へリダイレクト）

- `app/layout.tsx`: 全体レイアウト（言語設定を日本語に変更）

- `app/login/page.tsx`: ログイン画面

- `app/signup/page.tsx`: サインアップ画面

- `app/dashboard/page.tsx`: ダッシュボード（認証チェック付き）

- `app/dashboard/logout-button.tsx`: ログアウトボタンコンポーネント

- `app/test-supabase/page.tsx`: Supabase接続テストページ

- `lib/supabase/client.ts`: Supabaseクライアント（Client Component用）

- `lib/supabase/server.ts`: Supabaseクライアント（Server Component用）

**環境設定:**

- `.env.local`: Supabase環境変数

- `web-app/.gitignore`: 機密情報保護

#### 学んだこと

- Next.js 16 App Routerの基本（Server Component vs Client Component）

- Supabase認証システムの仕組み（BaaS、PostgreSQL、JWT）

- React Hooksの理解（useState、useRouter）

- Tailwind CSSでのダークテーマ実装

- 環境変数の正しい書き方（`.env.local`）

- トランザクションの概念（ACID特性）

- ログイン/サインアップ/ログアウトのフロー実装

- セキュアなルート保護（認証チェック）

#### 次回予定

**Phase 1: 基本UI実装（プロトタイプ）**

- ダッシュボードレイアウト構築
  - ステータスバー（ヘッダー）
  - タブナビゲーション（日誌/ToDoサマリー）

- 習慣チェックリスト実装

- ToDoカンバンボード実装（@dnd-kit/core）

- 日誌入力フォーム実装

**Phase 2準備:**

- データベーステーブル設計（profiles, daily_logs, habits, todos）



