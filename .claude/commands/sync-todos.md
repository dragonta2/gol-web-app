やりたいことリスト.md の未完了 ToDo を GOL-WEB Supabase に同期する。

以下のコマンドを実行してください:

```bash
cd /Users/ta2/ALL-DTA2/Develop/dta2/gol/web-app/gol-web && npx tsx scripts/sync-todos.ts
```

実行後、出力結果（作成件数・スキップ件数）を確認して報告する。
エラーがあればその内容も報告する。
